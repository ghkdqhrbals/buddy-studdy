# BuddyStudy Architecture

The Apple StoreKit membership payment ledger, transaction boundaries, event
sourcing rules, App Store notification authority, and operations setup are
documented in [BILLING.md](BILLING.md).

Outbound provider request/response retention, credential redaction, and
administrator inspection are documented in
[EXTERNAL_API_HISTORY.md](EXTERNAL_API_HISTORY.md).

The canonical iOS screen routes, compatibility aliases, push landing behavior,
and administrator-message destinations are documented in
[DEEPLINKS.md](DEEPLINKS.md).

## Overview

BuddyStudy is a SwiftUI app with shared domain logic across macOS and iOS. The app keeps lightweight local state for settings, drafts, and recoverability, but production question generation, answer grading, Voice Tutor call creation/sideband control and summarization, API-key validation, scheduled delivery, settings, records, and statistics are owned by the Spring Boot Kotlin backend. Study records and separate Voice Tutor learning results are not persisted in a local SQLite database; they are held as bounded in-memory view caches and refetched from the backend. The app never receives an OpenAI credential or calls an OpenAI REST API. For low latency, iOS sends foreground Voice Tutor media over an audio-only WebRTC peer connection negotiated by the backend, while every Realtime control event remains on the backend's authenticated sideband. Realtime frames are ephemeral unless the user explicitly opts into the separately gated private recording flow; consented calls are mixed locally and uploaded directly to private object storage after the call. iCloud/CloudKit state sync is no longer exposed or enabled; backend persistence is the active source of truth. Internal target names, bundle identifiers, background task identifiers, and legacy CloudKit record types retain `StudyMate` to avoid breaking existing installs.

The backend has one source tree and one container contract with two selectable
runtime artifacts: GraalVM Native Image (`native`, the production default) and
a regular Java 25 executable jar (`jvm`). Native builds use GraalVM 25; JVM
builds and execution use Eclipse Temurin 25. `BACKEND_RUNTIME` selects only the
Docker build stage. Both artifacts use the same ports, profiles, environment
variables, migrations, persistence adapters, and deployment workflow, so a
runtime comparison or rollback does not fork application behavior.

## Targets

- `StudyMateiOS`: iOS app and current public release target.
- `StudyMate`: macOS menu bar app, currently not shipped publicly while macOS update/sync UX is paused.
- `StudyMateTests`: unit tests for storage, OpenAI prompt/parsing helpers, backend routing, notification routing, and statistics logic.

## Main Modules

- `Models/StudyModels.swift`
  - Domain models: `StudySettings`, `Difficulty`, `QuestionItem`, `StudyRecord`, `GradingResult`.
  - Display strings through `AppStrings`.
  - Shared topic grouping through `TopicGrouping`.

- `ViewModels/AppState.swift`
  - Main `ObservableObject`.
  - Owns runtime state, drafts, selected tab, backend sync state, pending question limits, and user actions.
  - Coordinates backend API calls, local persistence, notifications, and timers.
  - Should delegate reusable decision logic to `Core/*` policies and backend action boundaries to `UseCases/*`.

- `Core/`
  - App-wide pure policies and shared decision rules.
  - `AppRuntime/AppActionRunner.swift` provides the common async action boundary for view-model orchestration: execute a use case, apply success state, route errors through app policy, and clear loading state from one repeatable shape.
  - New or modified async view-model flows should use the common action boundary unless they are pure local state mutations or low-level callback bridges.
  - `ErrorHandling/BackendErrorPresentationPolicy.swift` converts backend error codes into UI presentation, login, page-access, and identity-reset decisions.
  - `ErrorHandling/AppErrorHandlingPolicy.swift` maps backend error presentation into app UI behavior so login/device/token errors do not leak as repeated popups or feature banners.
  - Backend error parsing accepts both string error identifiers and numeric code ranges. Access-token expiry refreshes only the access token; only explicit device credential, device mismatch, or device-not-found codes replace the backend device identity.
  - Transient infrastructure failures (`408`, `429`, and `5xx`) and invalid backend responses keep their status, request ID, and diagnostic body in internal logs but resolve to localized, feature-specific retry guidance in user-facing UI. Structured non-transient business errors continue to use their server-provided messages.
  - Client-side decoding and cancellation errors are normalized by the same error policy so raw system messages such as missing-key decoding failures do not appear as repeated popups.
  - Protected tab UI must not pre-fetch page access. The app opens the tab and lets the requested backend API return auth, permission, or terms errors through the common error policy.
  - Required-term UI opens the document represented by the backend term identity and submits that exact `version` and `contentHash`; the backend rejects a mismatch before writing agreement history. New immutable policies are first registered with a future sentinel effective time, then activated by a separate post-approval Flyway migration after the capable iOS build is public and older builds are force-updated.

- `UseCases/`
  - Thin application action boundaries around backend capabilities.
  - `AppUseCases.swift` is the app composition root for use-case construction. `AppState` should replace this container when the backend client changes instead of constructing individual use cases in multiple places.
  - `Notifications/NotificationsUseCase.swift` centralizes backend notification list, unread count, individual/all read, delete, and clear operations.
  - `Records/RecordsUseCase.swift` centralizes backend record operations such as fetch, grading, draft saving, skipping, deletion, publicity, and full clear.
  - `Settings/SettingsUseCase.swift` centralizes backend settings, model option, API validation, and schedule sync requests.
  - `Stats/StatsUseCase.swift` centralizes backend topic statistics and activity requests.
  - `StudyRoom/StudyRoomUseCase.swift` centralizes study room backend operations such as study fetch/create/update/delete, quota lookup, and backend question creation.
  - `Community/CommunityUseCase.swift` centralizes public question, paginated liked-question collection, sign-in, profile, like, report, abusive-user blocking, and comment backend operations.
  - `ReferralUseCase` centralizes referral summary and fallback attribution recovery. Manual code redemption is restricted to the server-defined short sign-up eligibility window, and link capture plus new-account eligibility remain separate from ordinary existing-account sign-in.
  - `AppState` may still orchestrate state application and recovery, but it should not grow new direct backend action logic when a use-case boundary exists.

- `VoiceTutorView`, `VoiceTutorSessionView`, and `VoiceTutorViewModel`
  - Present Pro eligibility, server-owned remaining time, bounded session history, the live foreground lesson, and the derived Tutor Learning Result.
  - Use `BackendVoiceTutorStatus`, `BackendVoiceTutorQuota`, `BackendVoiceTutorSessionStart`, `BackendVoiceTutorSessionListItem`, `BackendVoiceTutorSessionPage`, `BackendVoiceTutorSessionDetail`, `BackendVoiceTutorSessionResult`, and `BackendVoiceTutorTranscriptTurn` as additive backend contracts.
  - Open only the authenticated BuddyStudy WebSocket with subprotocol `buddystudy.voice.v1`; the iOS process never receives the server's OpenAI Realtime credential.
  - Own microphone/audio-route and interruption state for the lifetime of a user-started call. The active play-and-record voice-chat audio session and UIBackgroundModes audio entry preserve microphone, media and control through app backgrounding and device locking. Explicit dismissal, end, audio interruption, logout or account replacement still closes the stream and requests idempotent session finalization.
  - `buddystudy.voice.operation` carries a session-monotonic sequence, safe operation ID/function name, started/completed/failed phase and server-measured `elapsedMs`. Native tool work and automatic question/grading reads emit their actual operation boundaries. iOS maintains at most eight active timers per connection attempt, measures ongoing elapsed time with uptime, retains up to 512 completed/failed entries for the conversation and closes the event gate at termination. Provider-authored metadata, arguments and results never enter this status display.
  - Manual canonical-question capture reads and saves the existing record-ID draft through AppState/SettingsStore. Finish, reviewed Submit and Skip travel as typed authenticated control events; the backend owns canonical submission, generation and quota operations.

- `Services/SettingsStore.swift`
  - Local persistence facade.
  - Stores settings, API keys, draft state, backend metadata, and exposes an in-memory record cache for the current session.
  - Does not trim records as a retention policy. The backend owns durable record history, while the app incrementally fills its in-memory cache from paginated responses.
  - Stores backend device registration and a stable installation identifier in the iOS Keychain. The registration request sends the identifier over TLS, request logs redact it, and the backend persists only its SHA-256 hash so repeated registration is idempotent.
  - Keeps a pending referral code across app launch, authentication, and the required-terms gate until the backend returns a terminal attribution result. The value is short-lived account-onboarding state, not proof that a reward is due; the backend owns the sign-up eligibility window.
  - Voice Tutor session pages and results remain bounded view state backed by the server. They do not create a second local durable record store. Editable canonical QUESTION answers reuse the existing record-ID-keyed answer draft repository; completed VOICE_TUTOR exchanges never become drafts.

- `Services/OpenAIClient.swift`
  - Contains shared prompt/body/parsing helpers only.
  - It has no runtime networking methods; app-side OpenAI network calls are intentionally unavailable.
  - Uses the configured supported model list from `OpenAIModelOption` for settings validation and backend payloads.

- `Services/CloudSyncService.swift`
  - Legacy CloudKit implementation retained for source compatibility.
  - iCloud/CloudKit sync is not exposed or enabled in the current iOS release path.

- `Services/NotificationService.swift`
  - Handles local notifications, notification actions, iOS remote notification bridge, and macOS study window foregrounding.

- `SentryMonitoring.swift`
  - Initializes the iOS Sentry SDK before app state is created.
  - Accepts only error and fatal events. Product analytics, logs, metrics, automatic session health, and performance tracing are disabled.
  - Keeps ordinary Session Replay sampling at zero and uploads the masked replay buffer only for error events. Text, images, request bodies, and network headers are not recorded in Replay.
  - PostHog is not linked, initialized, or supplied through the iOS release workflow.

- `AppAnalytics.swift`
  - Owns the iOS-only Firebase Analytics integration and exposes typed product events.
  - Uses `FirebaseAnalyticsCore`, disables advertising-network registration and automatic SwiftUI screen reporting, and never sets a Firebase user ID.
  - Rejects missing, placeholder, disabled, or bundle-mismatched Firebase configuration instead of starting partial collection.
  - Keeps local Debug collection disabled unless `BUDDYSTUDY_GA_DEBUG=1` is explicitly supplied.
  - The release workflow replaces the repository placeholder with the `GOOGLE_SERVICE_INFO_PLIST_BASE64` GitHub Secret before building.

- `backend/`
  - Spring Boot Kotlin APNs push backend.
  - Runs Spring WebFlux on Reactor Netty with Kotlin suspending controllers, use cases, persistence ports, and security lookups.
  - Uses Spring Data R2DBC and the MySQL R2DBC driver for runtime database access. Request-path persistence does not use JPA, Hibernate, Hikari, JDBC, or a blocking executor.
  - Uses reactive transaction context for suspending `@Transactional` methods. Entity mutation must be persisted explicitly because Spring Data Relational does not provide JPA dirty checking, lazy loading, or managed entity sessions.
  - Uses JDBC only during application startup when Flyway migrations are enabled. Runtime details and migration trade-offs are documented in [`R2DBC_MIGRATION.md`](R2DBC_MIGRATION.md) and [`WEBFLUX_MIGRATION.md`](WEBFLUX_MIGRATION.md).
  - The backend is organized as multi-module hexagonal architecture: `domain`, `application`, `infra`, and executable `tutor`.
  - Incoming web/scheduler/stream handlers live in `backend/infra`, persistence/OpenAI/APNs/Redis integrations live in `backend/infra`, and use-case services live in `backend/application` behind inbound port interfaces.
  - Spring Data Relational entities live in `backend/domain`; outbound adapters use coroutine repositories, `R2dbcEntityTemplate`, or `DatabaseClient` for explicit SQL.
  - Closed entity attributes such as provider, lifecycle status, language, platform, source, and notification type use Kotlin enums. Spring Data R2DBC stores enum names as `VARCHAR`; enums with lowercase external database codes use explicit reading and writing converters. Flyway migrations document every entity-backed table and constrained column with MySQL comments, list allowed enum values, and add matching `CHECK` constraints. Free-form text, identifiers, and extensible catalog codes remain strings.
  - Topic-level statistics calculation is separated into `backend/application/src/main/kotlin/com/buddystudy/backend/stats/StatsService.kt` and is consumed by study application services.
  - Public base URL: `https://api.ghkdqhrbals.org`.
  - Serves the canonical referral URL `https://api.ghkdqhrbals.org/referrals/{code}`. The associated-domain file lets an installed iOS app receive that path as a Universal Link; otherwise the public server landing page offers the App Store and a copyable code.
  - Runs behind Nginx on host port `443`.
  - API request, exception, and authentication logs share `ApiLoggingPolicy`. The `dev` profile emits compact method/path/status/duration logs without body capture, request IDs, IP addresses, headers, or full stack traces; production keeps detailed structured logs for operations.
  - Uses a private Dockerized MySQL container with a persistent named volume.
  - Separates OpenAI workload ownership. `SystemOpenAIClient` and
    `OPENAI_API_KEY_SYSTEM` serve only post-study child-topic suggestions.
    `OpenAIClient` and `OPENAI_API_KEY_USER` serve question generation,
    embeddings, translation, user-answer feedback, grading, and grading
    previews, plus Pro Voice Tutor call negotiation, sideband control, PCM
    fallback relay, and lesson summary. The
    same regular user-content key remains server-only for realtime work and is
    never returned to iOS or MCP clients; no third OpenAI key type is supported.
    Production startup and deployment validate the enabled workloads and their
    required credentials.
  - Answer submission and AI grading are separated by a transactional outbox. `POST /api/v1/records/{id}/answer` stores the immutable submitted answer, changes the question projection from `UNGRADED` to `GRADING`, appends the first durable `question_grading_events` event, stores its ID as `grading_last_event_id`, and writes the `ANSWER_GRADING_REQUESTED` outbox row in one transaction. It returns `202 Accepted` with `questionStatus`, `correlationId`, and `gradingLastEventId`, and never waits for OpenAI.
  - `AnswerGradingStreamListener` consumes the typed Redis domain event and runs evidence analysis, criticism, judging, and optional adjudication through `AnswerGradingService`. Each transition is persisted before it is exposed to clients; completion stores the AI decision and statistics dirty key atomically.
  - `question_grading_events` is the append-only grading lifecycle event store. Every row records both the detailed grading stage and the resulting question lifecycle state; `questions.status` and `questions.grading_last_event_id` are the current read projection updated in the same transaction. `GET /api/v1/answer-processes/{correlationId}` exposes request-scoped progress as a one-shot polling response. The iOS app sends the last durable event ID as `after`, receives all newer stages without gaps, and polls at the fixed three-second product interval while `questionStatus=GRADING` and the grading status is non-terminal. Leaving the screen cancels only status polling; an already-sent answer request is allowed to finish and persist its accepted state.
  - Stores generated questions in MySQL before sending APNs notifications.
  - Owns community profiles, public question browsing metadata, question reports, and persistent per-user block relationships. Authenticated public-question lists, detail reads, and comment lists enforce the block relationship server-side so reinstalling the app or using another device cannot restore blocked content.
  - Treats anonymous identities as installation credentials rather than administrator-visible members. Admin user and quota queries exclude `ANONYMOUS` rows.
  - Records referral attribution only for a newly created account while it remains `PENDING_TERMS`; a retry of that unfinished sign-up may retain the same first accepted code, but an existing `ACTIVE` account is never attributed. Required-term activation is the reward boundary: when that account becomes `ACTIVE`, one transaction creates the referral and grants the inviter and new member one month of Pro (`TIER2`). A unique referred-account constraint, self-referral validation, deterministic grant identities, and row locking make retries idempotent and prevent partial two-sided rewards. Rewarded attribution is retained after an inviter withdraws: nullable inviter references anonymize the departed account without erasing the surviving member's referral, grant, or one-time redemption claim. Manual code redemption is attribution recovery within the server-defined short sign-up eligibility window, not an eligibility path for existing accounts.
  - Public question views, likes, unlikes, comment creation, and comment deletion are transactionally written to `redis_event_outbox` and published to dedicated Redis Streams. Reaction tables remain the source of truth. View events update the idempotent `question_stats` read model, while reaction events are durably consumed into Inbox history without applying the already-committed reaction count a second time.
  - Transactional domain writes append `PENDING` typed events to `redis_event_outbox` in the same R2DBC transaction. Notification events carry their own `shouldPush` option, so inbox creation and optional push delivery cannot diverge through separate outboxes. After commit, `OutboxPublicationService` immediately claims, publishes, and completes each row. `OutboxRecoveryScheduler` uses that same flow for failures and abandoned claims, so polling is a recovery mechanism rather than the normal delivery path. Claim failures fail the managed job and retain their stack trace instead of being reported as a successful zero-row run.
  - `RedisStreamTopicManager` is the single Redis client boundary for topic registration, Spring Data Redis consumer-group reads, acknowledgement, retention, administrator inspection, and Lettuce `XAUTOCLAIM`.
  - Typed consumers declare topic, event type, Jackson payload class, group, consumer, batch size, and timing through `@StreamListener`. A handler is acknowledged only after Jackson conversion and handler completion.
  - Database outbox claims use a lease plus UUID fencing token. Immediate request publication and scheduled recovery may race, but only the token owner can mark a row published or retryable. See [`EVENT_OUTBOX_ARCHITECTURE.md`](EVENT_OUTBOX_ARCHITECTURE.md).
  - Redis consumer pending recovery is declared through `@StreamScheduler`; every registered topic has a matching scheduler in the same consumer group because `XAUTOCLAIM` cannot recover another group's pending list. Startup explicitly creates the named `-recovery` consumer so it is visible before the first failure. Entries older than the configured idle time are claimed and passed through the same typed handler contract.
  - A failed Stream handler receives at most three Inbox attempts. The third failure is recorded as terminal and acknowledged out of the pending list, but the Redis Stream entry is retained for operator inspection until normal bounded retention trims it.
  - Domain-event and push-delivery streams are atomically trimmed to independently configured exact maximum lengths. Both default to 1,000 entries. `redis_event_outbox`, `app_notifications` push state, and the consumer Inbox remain the durable diagnosis and recovery surfaces when bounded Redis retention advances.
  - Push delivery uses one consumer group with ten stable concurrent worker identities by default. Blocking Redis reads run on `Dispatchers.IO` instead of Reactor event-loop threads.
  - Redis delivery is at-least-once. `(event_type, event_id)` is the producer idempotency key, and consumers must keep their existing event-id deduplication because a crash can occur after Redis accepts an event but before the outbox row is marked published.
  - Outbox SQL uses jOOQ table and field classes generated from the ordered Flyway migration history, so table and column changes fail at compile/code-generation time instead of relying on untyped row maps. Reads use explicit field projections rather than generated mutable record construction so the same claim path works in GraalVM native images without reflective record creation.
  - Forwards reports by SMTP only when report-email secrets are configured; reports are still stored when email delivery is unavailable.
  - Stores studies as a MySQL adjacency list through `studies.parent_study_id`. Root studies use `NULL`; child depth is not capped by the schema or API.
  - Uses `sort_order` plus `id` for stable sibling ordering. A self-referencing foreign key cascades subtree deletion at the study layer, while question soft deletion resolves the same subtree with a recursive CTE before deleting the studies.
  - Keeps study identity on record responses so the iOS cache can remove only records owned by a deleted subtree, even when two branches use the same topic label.
  - Separates read contracts by purpose: `GET /api/v1/records/{recordId}` is record detail, `GET /api/v1/studies/{studyId}` is one study room with both its latest pending and latest completed question, and paginated `GET /api/v1/studies` is tree/list synchronization. The iOS room prefers the pending question and otherwise keeps the latest completed question, submitted answer, and AI response visible.
  - Root studies own schedule state and question-generation settings. Descendants own topic, difficulty, ordering, and `active_for_questions`; scheduled claims never target descendants directly.
  - Keeps three write boundaries explicit: `POST /api/v1/studies` creates a root, `POST /api/v1/studies/{parentStudyId}/topics` creates a descendant without generating a question or consuming quota, and `POST /api/v1/studies/{topicId}/questions` generates a question.
  - Authorizes study and topic creation with `study:create`, which has no monthly question requirement. Manual question generation uses the separate `question:create` permission and is the only one of these write paths guarded by monthly question quota.
  - Question generation is an asynchronous Choreography Saga. Submission reserves quota and stores the `QUEUED` Saga plus `QUESTION_GENERATION_REQUESTED` outbox in one transaction, then returns `202 Accepted` with a correlation ID. Generation and translation use separate Redis consumer groups and `(event_id, consumer_group)` Inbox leases; the canonical Saga advances through compare-and-set transitions. Every handler commits business state and its durable outbox before finalizing the Inbox lease, so a database rollback cannot acknowledge an uncommitted effect. A terminal generation or translation failure stores `FAILED` plus `QUESTION_GENERATION_ROLLBACK_REQUESTED`; its dedicated consumer removes any ungraded generated question and projections, reverses coverage, refunds quota, and sets `rollback_completed_at` transactionally. A failed process remains non-terminal and retains its topic lock until that compensation commits. The iOS client polls `GET /api/v1/question-processes/{correlationId}` and resumes a persisted process after restart. See [`QUESTION_GENERATION_SAGA.md`](QUESTION_GENERATION_SAGA.md).
  - Korean, English, and Japanese delivery preserves independent source languages for the question, user answer, AI response, and each comment. Question generation (manual or scheduled), answer submission, grading completion, and comment creation atomically append the missing per-language `QUESTION`, `ANSWER`, `AI_RESPONSE`, or `COMMENT` `CONTENT_TRANSLATION_REQUESTED` rows to the shared transactional Outbox. Publication starts after commit; a failed immediate publish is recovered by the normal Outbox worker. Localized reads retain read-repair behavior: a missing or stale translation returns the original immediately and idempotently requeues only the missing part. Translation uses the configurable provider chain while retries and terminal failures remain isolated through the Redis Inbox consumer. A durable request token deduplicates concurrent write-through and read-repair requests, while `PENDING` or `FAILED` work older than five minutes receives a new token. Production LibreTranslate runs as an internal-only deployment module on the backend Docker network; the backend keeps OpenAI as the next provider in the chain. See [`QUESTION_LOCALIZATION.md`](QUESTION_LOCALIZATION.md).
  - Selects the next eligible active node from the complete root subtree by oldest `last_sent_at`, with never-selected nodes first and stable `sort_order`/`id` tie-breaking. Nodes already at their per-topic pending-question limit are excluded before selection, preventing one unanswered branch from starving the rest of the tree; the root backs off only when every active node is blocked.
  - Stores both manual and scheduled questions with the root study ID while copying the selected node's topic and difficulty into the question. Inactive nodes remain available for manual generation.
  - `POST /api/v1/studies/{id}/topic-suggestions` requests unique GPT suggestions for a parent node, and `PATCH /api/v1/studies/{id}/question-activation` changes only rotation participation. The blocking OpenAI SDK call runs on the coroutine I/O dispatcher instead of a WebFlux request thread.
  - `system_topic_catalog` is the shared source for reusable topic suggestions. A lookup is keyed by normalized root plus the hashed ancestor path, language, and child depth. A cache miss invokes the system model, then idempotently stores the generated children. Provider failures return localized, parent-scoped fallback suggestions with `FALLBACK` or `CATALOG_FALLBACK` as the response source; transient fallbacks are not persisted into the shared catalog.
  - The managed catalog supports four descendant levels and up to ten children per opened branch. User study rows are materialized lazily from selected suggestions instead of eagerly cloning a combinatorial tree; newly created nodes remain active for question rotation by default.
  - Rejects duplicate topics using a trim, case-fold, and repeated-whitespace normalized key across all studies owned by the user.
  - Resolves monthly question allowance from `user_entitlement_projection`, active `user_memberships`, and the policy-v5 `user_quota` projection. A verified upgrade immediately raises the base limit without clearing committed usage, active reservations, or the current-period bonus. A downgrade remains pending without changing the current tier or quota until the first verified lower-tier renewal at or after `currentPlanEndsAt`; when effective it lowers only the base limit, preserves the counters, and clamps remaining capacity to zero when the preserved usage already reaches the lower limit. Generation reserves one unit by Saga correlation ID before work, commits it only after a usable question is stored, and releases it exactly once on permanent failure. Every accepted mutation updates `user_quota` and appends `user_quota_history` in one transaction, while `quota_reservations` retains its original period identity across rollover. A managed one-minute rollover resets the natural monthly window, and every quota read/write invokes the same idempotent rollover as a recovery fallback. `GET /api/v1/questions/quota` remains compatible while exposing base, bonus, reserved, period, and policy-version fields; `GET /api/v1/billing/status` is the unified entitlement-plus-quota authority.
  - Provides authenticated admin APIs for paginated user search, current-period bonus ledger adjustments, on-demand RevenueCat reconciliation, and a unified subscription/invoice/payment/quota timeline. Operators cannot assign a user's subscription tier directly.
  - Stores the latest iOS marketing version, build number, and observation time on the exact authenticated `devices` row. Every launch/foreground policy observation also appends an idempotent per-device `app_control_events` fact, so the current device version and the historical version/update funnel are both queryable. The legacy `POST /api/v1/app-updates/check` remains available for older builds.
  - Owns one active update campaign per platform and per-user campaign state in `app_update_campaigns` and `app_update_user_states`. Forced and optional presentation modes share the same localized payload; shown, dismissed, App Store opened, and target-version-returned timestamps remain distinct so conversion uses prompted users as its denominator.
  - Publishes one versioned JSON document to the Firebase Remote Config parameter `ios_app_control_v1`. The backend fetches the current template, merges only its owned parameter, validates it, and publishes with the fetched template version instead of force-overwriting unrelated Firebase parameters. The policy carries independent App Store/TestFlight update channels plus one current or scheduled maintenance window.
  - Exposes authenticated Monitoring APIs under `/api/v1/admin/app-updates` for campaign activation/ending, policy republishing, maintenance activation/ending, campaign metrics, and paginated user-level conversion inspection. Database state is committed before the external Firebase publication; publication status, revision, timestamp, and a bounded error are retained for operator recovery.
  - Provides authenticated feedback review and targeted messaging APIs. Feedback retains the submitting user/device target and progresses through `NEW`, `REVIEWED`, and `REPLIED`; administrative messages reuse the notification outbox/Redis/APNs pipeline instead of sending directly from the controller.
  - Validates administrative destinations as allowlisted `buddystudy://` deep links. The default `buddystudy://home/message` route opens a compact Markdown announcement sheet on Home, while record, statistics, settings, profile, and public-question destinations use the shared `AppRoute` parser. The legacy studies route remains readable but is not offered by the operator composer.
  - Exposes Redis Stream inspection under the unified Monitoring `Manage` section. The standalone analytics admin does not duplicate this operational surface.
  - Provides authenticated topic/key search, cursor-based Redis Stream inspection, exact stream-entry ID lookup, consumer groups, consumer Inbox attempts, and `redis_event_outbox`. Nested credentials are redacted before serialization. See [`REDIS_STREAM_OPERATIONS.md`](REDIS_STREAM_OPERATIONS.md).

- `Views`
  - `StudyView`: active question and pending question workflow.
  - `HistoryView`: 30-row incremental record/search pagination, detail, and deletion.
  - `StatisticsView`: shared-axis root-study growth comparison, calculation help, period filtering, a pinch-zoomable circular score tree without separate zoom buttons that reuses the My Studies layout and saved node positions, trend charts, and a compatibility projection for older servers. Selecting a tree node keeps the growth summary visible and appends an independently paginated 30-row record list for that exact `studyId`; selecting a row reuses the standard record detail.
  - `SettingsView`: macOS settings.
- `MobileRootView`: iOS tabs, profile category hub, settings, notification inbox, and study-tree interaction.
  - Firebase Remote Config is the control-plane read model for maintenance and update guidance. iOS fetches and activates on launch/foreground and installs Firebase's real-time config listener while the process is active. A pure resolver enforces `maintenance > forced update > optional update > normal`, validates schema and freshness, compares marketing version then build, and schedules a local boundary task for future maintenance start/end without polling.
  - Forced and optional updates share the same compact centered card and softly dim the underlying screen. Forced mode uses the stronger dim treatment as a full-screen interaction barrier, hides the dismiss action, marks the card as required, and keeps it present until the installed version reaches the target; optional mode uses a lighter non-intercepting dim, leaves underlying content interactive, and persists dismissal by campaign through `SettingsStore`. Consumer App Store apps cannot silently install their own updates; automatic installation timing remains owned by iOS/App Store settings. Maintenance is a full-screen gate and retains the hidden developer bypass. Malformed, expired, unsupported, or unavailable Remote Config fails open for maintenance and may use only the existing backend app-update API as an update-guidance fallback. There is no monitoring-owned status endpoint or maintenance polling path.
  - App Store CI replaces the tracked placeholder `StudyMate/GoogleService-Info.plist` with the release secret. Local signed device builds must pass `BUDDYSTUDY_FIREBASE_PLIST_PATH=/absolute/path/GoogleService-Info.plist`; the iOS target validates that file and installs it into the built app after resources are copied, without modifying or committing the tracked placeholder.
  - The primary tab bar exposes Home, Records, Statistics, and Notifications. Profile is pushed from Home in the existing navigation stack instead of being presented as a modal. Its top quota summary shows the resolved membership tier and opens Membership Management as a sheet; Settings remains a profile-hub destination so account and app preferences share one predictable entry point.
  - Home keeps a Voice Tutor phone action immediately before Search. Its navigation-owned quick-call intent consumes one fresh successful status response and uses the existing call-start policy; returning from the call, an unsuccessful refresh or a later status change cannot replay that intent. Quick entry defaults recording consent to false and prepares a missing authenticated profile before its status read without invoking the full profile-page/billing SDK preparation. That bootstrap permits an unresolved owner to become the verified owner within the same session, backend and language generations; cancellation and identity replacement still reject the result. Public-question row taps own detail navigation, while the shared ellipsis/context menu contains only available ownership/moderation actions.
  - Signed-in Profile exposes Liked Questions as a dedicated paginated destination. The app keeps this account-scoped list in an independent in-memory state store, reuses the public-question row/detail views, synchronizes successful like changes with the ordinary public feed, and clears the list at every session or backend-identity reset.
  - Avatar editing is a dedicated `Avatar` destination. Logout is the final destructive action in the profile hub, while irreversible membership deletion is isolated under `Settings > Account Settings`.
  - Public-question visibility is persisted through `PATCH /api/v1/profile` as `allowPublicQuestions`; it is independent of protected-page access policy.
  - Developer controls use a local build-distribution gate. Five taps on the Profile version row within two seconds grant access only when `AppDistributionContext` identifies a Debug or TestFlight build. TestFlight records the version/build that accepted the gesture; a different build clears the stale unlock and saved debugging toggle before startup. Debug builds retain the installation-level unlock, while App Store production always rejects and clears hidden developer access. The gesture reveals developer options but never opens the floating panel automatically. While the global maintenance gate is visible, five taps on the maintenance copy bypass only the current in-memory gate when access was already restored for that build; maintenance never grants developer access or changes the published Firebase policy. The floating APP/API debug panel uses a dedicated drag handle and remains inert until `debugPopupAllowed` is granted.
  - Backend selection is configuration-driven, not receipt-driven. The iOS Debug build setting embeds `https://lowfidev.cloud`; Release embeds `https://api.ghkdqhrbals.org`, so TestFlight and App Review use production by default. Resolution order is an explicit Xcode launch override, an unlocked debugging selection, the bundled build URL, then the production fallback. Release CI overrides the archive setting with the production URL and fails before upload unless both the archive and exported IPA `Info.plist` contain that exact value.
  - Per-topic question generation exposes its category-scoped in-flight state so only the selected study room renders the inline loading message.
  - The compact My Studies outline swaps branch data in one view, then reveals only the new row contents with a subtle direction-aware stagger. Row frames, dividers, and the card remain outside the animation, and content never becomes fully transparent or overlaps. Root expand/collapse stays immediate. Its card context menu exclusively exposes the View Full Tree destination alongside Edit and Delete, so the list does not duplicate that navigation row. View Full Tree remains available for childless roots as the entry point for adding the first child, while only the empty child-topic section is omitted. A selected topic's study screen exposes New Question plus a separate trailing More menu; that menu reuses the topic editor and resolves View Full Tree to the topic's containing root. Deletion is intentionally available only inside the editor.
  - My Studies derives root cards only from backend rooms whose `parentStudyId` is absent. Before the first hierarchy response it shows loading, and a failed response exposes retry instead of falling back to the persisted flat category cache. An already loaded hierarchy remains visible during ordinary refresh failures.
  - Recommended child topics support multi-selection. iOS filters duplicate normalized names, creates the selected topics in stable order through the existing single-topic use case, and refreshes the study tree once after the batch so topic creation remains separate from question generation.
  - `POST /api/v1/studies/{parentId}/topics` is retry-safe for an already-created normalized topic under the same parent: it updates and returns that node with `200` instead of turning an acknowledged-but-response-lost retry into `409`. The same normalized topic under a different branch remains a conflict because study topic names are unique per user.
  - The notification inbox calls `POST /api/v1/notifications/read-all` and updates loaded rows through `NotificationStateStore` only after the backend mutation succeeds.
  - A system notification tap targeting a detail selects the Notifications tab before publishing its route request. `MobileNotificationsTab` consumes that request and pushes one direct destination. Home and the legacy study-list route bypass the notification stack; the latter selects the hierarchy-aware My Studies scope already owned by `MobileHomeView`, with no parallel flat list destination. Home no longer stages an inbox destination and a nested detail destination in the same SwiftUI update. Notification record loading has explicit loading, unavailable, and retryable-failure states, reuses the first fresh record response without a duplicate detail fetch, and exposes Skip for ungraded questions.
  - `ADMIN_MESSAGE` notifications with `buddystudy://home/message` are a distinct route: an explicit APNs or notification-inbox tap selects Home and presents the stored Markdown in a sheet. The payload parser takes title/body from `aps.alert`, preserves a pending route until `AppState` is configured, and marks a linked inbox notification read after presentation. Other administrative destinations continue through `AppRoute`.
  - Answer drafts and submitted answers are different state domains. A draft is stored only in the record-ID-keyed draft repository and may update the current editor session; it never mutates `StudyRecord.answer`, the record cache, or canonical sync state. `StudyRecord.answer` becomes non-null only from an answer explicitly submitted through a notification action or from a backend response that has accepted the answer. UI presentation derives editor-versus-message state from that invariant, so background refreshes and draft debounce saves cannot promote an in-progress draft into a submitted message.

## Markdown Message Contract

- `question`, `expectedAnswerHint`, grading `feedback`, and grading `explanation` are stored and returned as Markdown source. Existing plain text remains valid, so this contract does not require a schema migration or a parallel HTML field.
- The backend asks the model for a restrained Markdown subset: paragraphs, emphasis, lists, inline code, and fenced code blocks. Generated HTML is not part of the contract.
- The backend stores model Markdown exactly as received; it does not normalize or rewrite generated content before persistence. iOS renders the stored source with MarkdownUI, while the backend uses commonmark-java only to derive notification-safe plain text.
- iOS permits only `http` and `https` links from rendered Markdown. Unsupported link schemes remain visible as text without an active link.
- Dense list rows use a plain-text projection for predictable height. Full question, hint, answer, feedback, and explanation bubbles render Markdown.
- APNs, the notification inbox, and local notification bodies use a parser-derived plain-text projection of the stored Markdown so notification surfaces do not expose formatting markers. The notification event keeps the Markdown source, and the push adapter derives the safe title/body projection from the persisted notification when delivery is claimed.

## Data Flow

```text
https://api.ghkdqhrbals.org/referrals/{code}
-> installed iOS app receives the Universal Link; an uninstalled client receives the App Store/code-copy landing page
-> iOS retains the normalized code through new-account authentication and the required-terms gate
-> the account must be new and still PENDING_TERMS; an existing ACTIVE account is not attributed
-> accepting every required term changes the new account to ACTIVE
-> one locked backend transaction creates the referral and grants both users one month of TIER2
-> duplicate delivery returns the existing result without creating another referral or either grant
```

Universal Links do not by themselves preserve attribution through an App Store
installation. Without an external attribution provider, the landing page's
copyable code and short-window manual attribution recovery are the supported
fallback.

```text
Manual action / backend scheduled interval
-> backend request transaction reserves quota and stores Saga(QUEUED) + outbox
-> manual API returns 202 Accepted + correlationId
-> generation consumer claims its Inbox row and advances Saga to GENERATING
-> OpenAI runs outside the database transaction
-> generated question + QUESTION_GENERATED outbox + locale translation outboxes advance Saga to TRANSLATING
-> translation consumer resolves topic/question/hint through the configured provider chain
-> localization snapshot and delivery outboxes are persisted in one transaction
-> Saga advances to COMPLETED
-> iOS polls by correlationId and caches the returned StudyRecord
-> current question updates only when it will not replace an active ungraded draft
-> notification/push outboxes deliver asynchronously
```

```text
Terminal generation/translation failure
-> failed-step transaction stores Saga(FAILED) + QUESTION_GENERATION_ROLLBACK_REQUESTED outbox
-> rollback consumer removes ungraded question/projections and reverses coverage
-> same rollback transaction refunds monthly usage and records rollback completion
-> process becomes terminal and the topic is available for another question
```

```text
User opens Pro Voice Tutor without selecting a study
-> status and session creation check entitlement and atomically reserve independent voice seconds
-> iOS negotiates audio-only SDP and authenticated control using X-Voice-Turn-Protocol: realtime-native-v1
-> backend creates the regular-key OpenAI Realtime call and confirms explicit turn_detection=null
-> local Silero reports paired speech edges; the backend commits input and schedules realtime responses
-> normal replies use the model's live conversation immediately, without final-ASR/SQL/classifier gating
-> original USER/TUTOR transcripts persist asynchronously with provider identities and fixed source lesson epochs
-> tutor asks what to discuss without a self-introduction, then uses actual owned study reads
-> the realtime model selects the learner's exact saved topic through select_voice_study
-> one owner-locked transaction verifies the current call, latest source USER, exact candidate, full parent path and current revision
-> the returned frozen focus/level controls subsequent questions; guided advance moves one actual direct-child edge
-> for a requested create/edit/delete, the model calls prepare_voice_study_mutation
-> the server freezes one expiring session-bound action/target/patch and returns its natural confirmation question; no write occurs
-> tutor asks once; after actual playout and a new natural learner reply, the model calls confirm_voice_study_mutation with only proposal_id and confirm
-> durable exact question/USER boundaries plus live scope/revision and target checks fence that tool; a tutor preamble does not revoke the latest USER
-> refusal cancels; agreement consumes the immutable ticket once before the canonical owner-scoped create/CAS-update/subtree-delete
-> tutor briefly reports the verified result; an already-chosen new/revised topic can be selected on the same learner turn without another readiness prompt
-> one level-appropriate substantive question, complete answer and brief feedback are followed by space to respond
-> app background/lock preserves the live audio/control session and quota clock
-> explicit end/dismissal, interruption, quota deadline or terminal failure settles server-observed connected time once
-> post-call evidence analysis identifies genuine complete Q&A/feedback using the raw transcript and immutable focus/revision ledger
-> setup and topic-management talk never becomes scored progress; verified exchanges use the existing canonical VOICE_TUTOR record/tree/publication path
-> paginated session history remains private source evidence; no ordinary question quota or active answer draft is changed
```

```text
User answer
-> AppState saves answer draft
-> AppState.gradeCurrentAnswer or gradeRecord
-> RemotePushBackendClient.gradeRecord
-> backend atomically persists the submitted answer, grading correlation,
   questionStatus=GRADING, gradingStatus=QUEUED, and answer translation outboxes before OpenAI work
-> leaving the detail screen cancels only client polling
-> reopening fetches /api/v1/studies/{studyId}, never the full tree page
-> iOS merges pendingQuestion answer/grading state over stale local cache
-> when pendingQuestion is absent, iOS displays latestQuestion with the submitted answer and AI response
-> iOS resumes polling by the persisted grading correlation
-> backend calls OpenAI and persists score, feedback, explanation, and AI-response translation outboxes
-> SettingsStore updates StudyRecord
-> StatisticsView recalculates topic ranges from records
-> backend stats are refreshed from MySQL records
-> GET /api/v1/stats/studies joins graded question study IDs to the current study tree
-> root and parent nodes aggregate capped descendant weights
-> iOS reuses the My Studies circular tree geometry to show the root aggregate and every descendant's individual score
```

```text
My Studies root
-> GET /api/v1/studies
-> app builds parentStudyId adjacency map
-> selecting a room calls GET /api/v1/studies/{studyId} for its question state
-> compact branch navigation swaps data once, then reveals row contents without moving row geometry
-> recursive tree layout renders circular nodes with restrained level colors
-> the user can switch orientation, pinch or button zoom, drag nodes, and reset saved positions
-> selecting a node opens the root-owned question page with that node as the manual topic
-> plus opens GPT recommendations first, supports selecting multiple suggestions with one shared difficulty, and keeps manual entry as an explicit alternative
-> adding a root calls POST /api/v1/studies
-> adding a child calls POST /api/v1/studies/{parentStudyId}/topics with sortOrder, topic, level, and initial question activation
-> child creation cannot call OpenAI and cannot reserve monthly question quota
-> node activation controls scheduled round-robin participation without disabling manual generation
-> tree options support multi-select activation, pause, and subtree deletion
-> deleting a node soft-deletes questions for the resolved subtree, then cascades the study subtree
```

```text
Root schedule becomes due
-> backend claims the root only
-> complete subtree is resolved
-> active node with the oldest lastSentAt is selected
-> the backend-owned prompt and root OpenAI model generate a question for the selected node's topic and level; iOS study editors do not expose prompt overrides
-> question is stored under the root study ID
-> selected node lastSentAt and root nextDueAt are advanced atomically
-> Redis outbox publishes the APNs notification
```

```text
Profile > Usage appears or a quota-related request fails
-> GET /api/v1/questions/quota
-> active membership tier + optional user override determine monthlyLimit
-> monthly usage determines remainingCount
-> app renders remaining/monthly/reset only in the Usage category without exposing the internal plan
-> QUOTA_EXCEEDED routes through the shared app error policy and shows reset time inline
```

```text
Public community feed

- WebRTC output-stop may arrive just before `response.done`. A learner turn that starts in that narrow interval is held at the input-commit boundary until the matching response finalizes, then receives the exact tutor item, spoken target offer and question/answer/feedback identities. It is never rebound to speech that began before output stop or to another response generation.
- Long continuous speech keeps each provider transcript item intact. Only a checkpoint already classified meaningful and acknowledged through the publish path contributes bounded, same-sequence assessment-only context to the final tail. Deleted/non-communicative checkpoints are excluded, and neither checkpoint nor final transcript persistence is rewritten.
-> GET /api/v1/public/questions?tl=ko|en|ja&view=localized|original
-> GET /api/v2/public/questions?tl=ko|en|ja&view=localized|original is the current iOS feed contract and returns an ordered items[] list of PUBLIC_QUESTION and, at most once, NATIVE_AD_SLOT
-> GET /api/v2/public/questions/search?query=...&tl=ko|en|ja&view=localized|original
-> authenticated GET /api/v1/public/questions/liked?query=...&tl=ko|en|ja&view=localized|original returns only the viewer's still-public liked questions in bounded pages and never inserts native advertisements
-> tl (`ko|en|ja`) takes precedence over the deprecated language alias
-> view defaults to localized; original bypasses translation scheduling
-> missing translations return the original plus PENDING and enqueue content-translation
-> canonical questions and comments keep original text only
-> READY question/answer/AI snapshots are projected into question_search by language
-> comment translations remain isolated in question_comment_localizations
-> only READY translation snapshots are exposed for the requested language
-> v2 considers a COMMUNITY_FEED slot only on the first unfiltered page and only for anonymous or effective TIER1 access; active TIER2/TIER3 access remains ad-free through the end of a scheduled downgrade, and an unresolved entitlement suppresses advertising
-> the common placement policy begins OFF and controls enabled/schedule, per-user UTC daily cap, minimum delivery gap, minimum question count, and insertion bounds; the server always enforces at least two preceding questions, one following question, and a minimum 60-second gap
-> slot eligibility and per-user/placement delivery state are reserved under a transactional lock, so simultaneous requests cannot exceed the cap; slot and delivery history cascade when the owning user is deleted
-> v2 search, liked-question pages, and offset pages after the first never receive a slot
-> after a successful current-launch UMP refresh/form flow and `canRequestAds`, iOS requires IAB TCF Purpose 1 consent when `IABTCF_gdprApplies` says European regulations apply; outside that case it accepts UMP's non-European decision, including the GPP signal written for a US-state message, then makes one non-personalized, teen-rated AdMob load without ATT/IDFA or BuddyStudy user ID, device ID, query, topic, content URL, or keywords; it does not request Limited Ads after European Purpose 1 denial/unknown state or UMP error and it neither refreshes automatically nor retries immediately
-> a successful AdMob response inside five seconds owns the slot; privacy-gate failure, error, no-fill, or timeout makes one POST /api/v2/native-ad-slots/{slotId}/fallback request, while consent-generation checks discard cached or late AdMob results created before a privacy-choice change
-> POST /api/v2/native-ad-slots/{slotId}/impression and /click idempotently record AdMob delegate callbacks with provider ADMOB; first-party fallback ads continue using the legacy selection impression, view, and not-interested endpoints
-> GET/PUT /api/v1/admin/native-ad-placement-policies/COMMUNITY_FEED owns the shared slot policy and 30-day slot, AdMob, and fallback metrics displayed by Monitoring's Advertising page
-> the v1 compatibility feed still reads active native_ad_campaigns, removes campaigns found in the current user's native_ad_campaign_suppressions, and applies audience, schedule, daily-cap, minimum-gap, and post-view cooldown gates
-> the adapter loads per-user frequency/activity signals and 30-day campaign delivery/open/not-interested signals with bounded batch queries rather than campaign-count-dependent queries
-> eligible campaigns are ranked by priority, authenticated/anonymous relevance, Bayesian-smoothed 30-day destination-open rate, freshness, exploration bonus, and Bayesian-smoothed not-interested penalty
-> 85% of selections use the top-ranked campaign; 15% explore one of the remaining top-three candidates
-> the backend chooses a bounded position after the first two question rows and before the page tail, then persists native_ad_selection_history
-> the v1 response contains one final ordered items[] list with type PUBLIC_QUESTION or ADVERTISEMENT; questions[] remains a compatibility field for older clients
-> ADVERTISEMENT carries selectionId, campaignId, provider name, localized advertising label/title/body/full affiliate disclosure, optional Coupang CDN image, and a validated BuddyStudy deep link or HTTPS Coupang destination
-> the iOS advertisement row mirrors the public-question card hierarchy, uses the provider as the topic meta, renders the normalized full affiliate disclosure in a compact neutral notice band immediately below that meta and before promoted content, adds a trailing thumbnail only after a valid HTTPS image succeeds, and collapses to a full-width text row when the image is absent or fails
-> iOS renders items[] unchanged, routes buddystudy:// through AppRoute, and opens validated HTTPS destinations externally; it performs no ranking or placement
-> tapping an advertisement calls POST /api/v1/native-ad-selections/{selectionId}/view immediately before destination routing
-> keeping at least 50% of an advertisement row visible for one second calls POST /api/v1/native-ad-selections/{selectionId}/impression; ownership checks plus `coalesce(impression_at, at)` make repeated viewport reports idempotent
-> choosing Not interested removes the campaign immediately in iOS and calls POST /api/v1/native-ad-selections/{selectionId}/not-interested; the backend validates selection ownership and idempotently persists a user-scoped permanent ranking exclusion; iOS restores the exact removed row when authentication or persistence fails
-> the request validates selection ownership and appends one stable native-ad-view-{selectionId} Outbox event
-> community.native-ad.view.v1 delivers NATIVE_AD_VIEWED at least once; the Inbox consumer idempotently records viewed_at
-> server-side selection/view history is the sole ranking evidence and is safe across reinstallations and multiple devices
-> GET/POST/PUT /api/v1/admin/native-ad-campaigns is the authenticated operator source of truth for localized copy, mandatory full affiliate disclosures, Coupang CDN image, Coupang URL, schedule, audience, ranking weights, fatigue limits, and feed-position bounds; GET accepts optional query, ACTIVE/PAUSED/SCHEDULED/ENDED status, exact audience, limit, and offset filters whose count and page share one evaluation instant
-> the admin campaign response exposes the exact ranking coefficients plus 30-day feed-delivery selections, destination-open events, open rate, not-interested count, and not-interested rate so the operator UI does not duplicate ranking constants or overstate deliveries as verified impressions
-> GET /api/v1/admin/native-ad-campaigns/{campaignId}/users groups selection history by user with query, OPENED/NOT_OPENED, limit, and offset controls
-> the per-user response reports selection/open counts, first/latest selection, latest open, and distinct-device count; anonymous and withdrawn identities are redacted and raw device identifiers never leave the backend
-> POST /api/v1/feedback accepts only content and stores it in the dedicated feedbacks table
-> authenticated user and registered-device identifiers are captured as server-side metadata
-> Monitoring GET /api/v1/admin/feedback provides paginated operator review
-> PATCH /api/v1/admin/feedback/{id}/review records review state
-> POST /api/v1/admin/feedback/{id}/notifications queues an ADMIN_MESSAGE for its captured target
-> POST /api/v1/admin/users/{id}/notifications queues the same message for a selected member
-> notification outbox and Redis deliver APNs with parser-derived plain text
-> an explicit tap routes to the validated app destination; home/message presents the full Markdown popup
```

## Pro Voice Tutor

- During manual answer capture, a separate pause/continue control sits directly
  below the Finish Answer orb. It invokes the existing `pause-v1` path, never
  answer finish/submit. Pending acknowledgments disable repeated actions; the
  existing answer ID, recognized tail and edited draft survive the break. One
  rendered control follows compact/transcript anchor interpolation alongside
  the orb, with coordinated color/content transitions and Reduce Motion support.
- `request_user_input` is an asynchronous, server-owned provider-tool continuation.
  Only a control connection advertising `X-Voice-User-Input-Protocol: user-input-v1`
  can enable it; old native clients retain the prior catalog and cannot enter
  a form they cannot answer.
  `buddystudy.voice.user_input.request` carries a UUID request/session/attempt,
  a monotonic sequence and 1–5 typed questions (single/multiple/text), each with
  at most eight option IDs. `submit` and `cancel` echo those exact identities;
  no local edit or send releases the microphone. Matching server `state`
  acknowledgments apply submitted/cancelled or retain pending drafts after
  INVALID_ANSWERS/ACTION_FAILED. A newer call/account fences old events.
- A successful submitted form is appended as a new USER row through the common
  transcript store, with a reserved server-owned item identity and additive
  `STRUCTURED_INPUT` source. Selected labels and custom text are preserved;
  audio transcripts are never rewritten. Only durable persistence followed by
  the exact provider function-output acknowledgment advances the native human
  boundary, so successive GUI choices can select different saved nodes. Cancel
  and failed persistence cannot reuse an older input's focus authority. Generic
  forms do not supply spoken mutation confirmation or graded-answer evidence.
- iOS renders request cards below the conversation, with native multiline text,
  explicit submission and completed/cancelled receipts. Its microphone gate
  composes user-input waiting, manual answer review and acknowledged pause.
  MCP entries remain beneath their originating message or submitted card with
  seconds/minutes durations; only safe function metadata enters them.
- The native controller emits `buddystudy.voice.operation.context` once before
  each unchanged six-field operation-start event. The display-only context has
  `operationId` and optional `responseId`, `learnerItemId`, `tutorItemId`, `answerId`, frozen
  when the model requests work. Learning polls inherit the original request's
  context. The authenticated event policy strips other fields and rejects
  malformed identifiers; provider-originated context is not forwarded.
  Old clients ignore this separate event and retain existing status handling.
  iOS retains response/item identities on caption commits, including all source
  segments of submitted answers, and resolves operations beneath the exact
  response, provisional response, learner caption or submitted input card.
  A canonical answer's ID binds submission and grading to that draft or submitted
  answer even when it was typed entirely without audio transcription.
  Completion only changes status/duration. Older-server operations retain their
  initial local caption/provisional-response position; trimmed origins do not
  acquire a newer message. An unresolved exact origin waits for its caption/card
  instead of borrowing the current message; a trimmed origin leaves the visible
  transcript window with its operations. Context buffers and operation history remain bounded
  to one connection attempt. Only active operation rows run a periodic clock.
- Interruption and pause wait for a short observed PCM gap for at most 450 ms
  before the existing response cancellation/clear handshake. The capture loop
  continues and the ordered speech/pause stream preserves input fences. The
  native continuous RTP track does not expose word timestamps; this is a
  bounded acoustic opportunity, not exact word or sentence completion.

`VoiceTutorSessionView` automatically dismisses after the view model reaches
`.ended`, once final playout, recording and server settlement work has completed.
`VoiceTutorCallCompletionNavigation` defers this navigation while the scene is
inactive and applies it when the app becomes active. `.ending` keeps ownership
of in-flight cleanup, and `.failed` retains its visible error/retry controls.

### Active realtime-native conversation path

`realtime-native-v1` is the current iOS SDP/control capability. Its native sideband controller keeps OpenAI's realtime conversation as the conversational authority: that model interprets context, replies, chooses saved topics, and calls the native tool catalog. It does not run `VoiceTutorInputTurnCoordinator`, live meaningful-input/intent/consent classifiers, or separate question/feedback assessments. The legacy classifier/lease implementation described later in this section remains compatibility-only for `local-vad-v1`/PCM; it must not silently become a gate on a native reply.

- Native silent completion settles the exact frozen client speech sequence whenever no audio or tool continuation remains. Nonempty text-only assistant output violates the requested audio modality and receives at most one response retry for the unchanged input/revision; empty/noise output stays silent. Exhaustion sends `buddystudy.voice.input.retry` with an additive nonnegative `sequence` and optional exact `abandonedResponseId`. Zero is opening-only. iOS accepts it only for that pending input or matching active response; unannounced failures can therefore clear preparation without abandoning a newer turn. Native late ASR does not clear the retry hint. Diagnostics include bounded response IDs/sequences and disposition, never transcript text. Legacy unscoped retry events remain compatible.
- Cancelled or failed response function items that were never registered for execution receive bounded, correlated `executed:false` error outputs. Their exact output acknowledgments gate subsequent responses without scheduling a stale continuation. Accepted or completed calls cannot be overwritten or replayed. Saved-topic selection is a synchronous operation; a final error or an unexecuted function item is not a background selection job. The model must carry a contextual agreement into the exact saved-topic selection and canonical question flow rather than narrating a nonexistent pending operation. Question generation and grading may independently return real pending process identities.

- Media is still audio-only direct WebRTC. iOS uses the pinned local Silero detector (80 ms onset, 1,280 ms quiet release); backend-confirmed provider turn detection remains explicitly `null`. Continuing speech resets the entire 40-window quiet hold, including the probability continuation band below onset. The backend commits paired acoustic turns and requires another 400 ms of quiet before dispatching a normal response; a fresh speech start invalidates that deadline. Initial readiness, tool continuations and retries use the same quiet gate, and the controller tick re-evaluates it after the deadline even when no provider event arrives. Outside a held canonical-question answer, a normal response waits for committed input, but not ASR or persistence. An active manual answer capture blocks every ordinary response until explicit submission or skip completes. The existing quota terminal boundary remains exempt from the normal quiet gate. Local capture remains duplex, and explicit pause and foreground lifecycle stay authoritative. Native initial setup and the sideband update request explicit `audio.input.noise_reduction: null` for the device-processed microphone input. The handshake requires that effective null value and rejects later drift; legacy protocols retain their noise configuration. See the [ASR origin investigation](voice-asr-origin-2026-09-10.md) and [correction verification](voice-input-origin-fix-verification-2026-09-10.md).
- Returning learner speech interrupts any ordinary tutor response, including already announced or playing output. iOS first waits for the bounded local acoustic gap described above, then suppresses that response while continuing microphone capture and RTP rendering. The backend cancels ongoing generation by exact provider response ID, waiting for correlated creation when necessary, and clears the output buffer once audio is known. A definitively silent cancellation sends no clear; every issued clear must receive its exact acknowledgement before another response can begin. Interrupted text and tools cannot be forwarded, persisted or executed, and stale continuations do not consume the provider-error retry budget. Delayed commits from superseded speech retain evidence without re-arming an old reply. A displaced opening yields to the newest input. Fresh replies still observe the quiet gate and manual-answer submission policy above.
- The additive sanitized control event `buddystudy.voice.response.interrupted` contains only its type and exact `responseId`. The client fences that response so delayed creation/output/completion events cannot resume its sound or speaking indicator; normal output resumes only for a fresh response. Interrupted transcript evidence, pending tool work and response retries are discarded. Completed side effects are never replayed. The existing positive numbered `buddystudy.voice.input.speech.started`/`.stopped` protocol still carries learner boundaries; iOS does not send provider cancellation or clearing commands directly.
- Trusted realtime instructions distinguish an acoustic stop candidate from a completed thought: trailing clauses, word searches and breaths should yield an empty response without a prompt, acknowledgement or tool, while a complete short answer remains valid. This uses the existing conversational model, not another live classifier; long thinking pauses can still exceed the acoustic window and semantic completeness is not guaranteed by timing alone.
- A successful empty native response with no audio or pending tool continuation emits the server-only `buddystudy.voice.input.settled` control event containing only `type` and an exact Int64 `sequence`. Zero settles only the initial opening expectation; a positive value is the client acoustic sequence frozen into that response, independent of transcript order and any newer overlapping input. Reused short audio tails retain their latest client acoustic sequence. iOS clears only the matching learner wait and never clears newer pending input or active speech from a late settlement. An exact positive settlement also consumes an unannounced opening expectation displaced by the learner; a later ready event cannot revive it. Tool-only responses keep waiting for continuation; failed, quota, goodbye, and draining responses cannot settle input. Provider-authored and client-authored settlements are rejected, and the final control sanitizer drops invalid sequences and extra fields. Native provider metadata remains string-valued, while the correlated controller-owned quota announcement is a separate root Boolean preserved by the WebRTC sanitizer.
- Native USER/TUTOR transcripts retain their original provider item IDs, server-ordered rows and source lesson epochs, and are persisted asynchronously. No regex, short-word exclusion list or second GPT classifier removes input from the model's live conversation. Post-call evidence analysis identifies remaining eligible voice exchanges before they join canonical records; source rows used to answer an existing canonical QUESTION are excluded from that extraction. Setup and mutation discussion remains non-learning evidence.
- The native MCP catalog uses the same authenticated owner-scoped handlers for reads. It exposes `select_voice_study` and `advance_voice_study` directly to the realtime model, without separate spoken-offer attestations. `VoiceTutorLessonFocusPort.focusFromRealtimeModel` retains a transactional active-call/device check, exact live owned candidate and full parent-path validation, direct-parent validation for advance, metadata revision fencing, bounded snapshots and the unique source USER row fence. Selecting a topic does not create a question or spend question quota.
- `VoiceTutorCanonicalQuestionCoordinator` binds pending questions and process identities to the current owner, device, authentication session, provider call, exact study and lesson revision. Successful selection first uses canonical `list_pending_questions(study_id)`; `QuestionPort.findPendingByUserAndStudyId` applies both owner and exact study predicates before count and pagination, including non-page-aligned offsets. Only nondeleted, nonskipped QUESTION rows in UNGRADED/GRADING remain pending. A saved unanswered question keeps its own original text, topic and difficulty rather than the selected node's newer level. Compact tool output omits hints and rubrics; typed saved-question readback metadata supplies the native controller's question to speak.
- The native catalog also exposes bounded `request_question`, `get_question_process`, `skip_question`, `submit_answer` and `get_grading_process` operations through the existing canonical MCP handlers. An existing unanswered question prevents new generation; an incomplete pending page cannot be treated as empty. A genuine new request uses ordinary generation/quota and a server-derived idempotency key scoped to the exact session, study and learner turn. Only that request's returned correlation may be polled. Explicit skip targets the currently bound question after a fresh learner turn, creates no replacement and consumes no question allowance. Under the existing record row lock, skip accepts only an unsubmitted UNGRADED question, repeated SKIPPED requests preserve timestamps without another save, and answer/queue cannot revive a skipped question.
- A trusted canonical saved-question readback arms a server-owned answer ID bound to the call, study, record and lesson revision. After actual question playout, `buddystudy.voice.answer.state` opens `listening`. Acoustic stops and 20-second checkpoints commit recognition only: capture, finalizing, review, submission and retry states suppress ordinary response dispatch and model tools. iOS synchronously mutes capture and enqueues its final VAD stop before `buddystudy.voice.answer.finish` in the same control FIFO. The controller waits for pending commits, final ASR and persistence before publishing `review`; a bounded recognition timeout keeps manual editing available and marks source evidence incomplete.
- `buddystudy.voice.session.state` is an additive, authenticated native-sideband presentation snapshot with a monotonic call sequence, lesson revision, separate pause flag, known phase and optional scoped study/record/answer identities. The native controller publishes it from its own lifecycle and typed canonical-operation metadata; model text and provider events cannot assert a trusted phase. iOS fences stale sequences/revisions and ended attempts, gives transport/pending-control states display priority, and never treats this display snapshot as permission to unmute or submit. The complete phase contract and verification are documented in [voice-session-state-verification-2026-09-10.md](voice-session-state-verification-2026-09-10.md).
- Ordered, deduplicated `buddystudy.voice.answer.transcript` parts extend one editable draft. iOS keeps user edits authoritative, ignores stale answer IDs/records/revisions and final parts after review, and holds the microphone during review/submission/failure. The current answer card replaces its raw learner bubbles. Existing local drafts are loaded through AppState and SettingsStore; recognition alone does not replace an existing stored draft. Explicit edits and submission persist the current reviewed text without selecting a different question. Empty or over-8,000-UTF-16-unit text cannot submit; overflow remains editable. Skip uses the same typed control path and preserves drafts. `VoiceTutorAnswerEditor` presents the existing draft binding in a large sheet with one flexible-height native `TextEditor`, outside the transcript ScrollView and its drag/auto-follow behavior. Opening it cancels pending auto-follow and scroll-settlement tasks; both remain gated while editing. The sheet is bound to the current answer ID, rejects writes for a replaced or non-editable answer, and closes on identity or terminal phase changes. Done only dismisses the sheet, preserving the existing SettingsStore draft path and explicit-submit requirement.
- Model-originated `submit_answer` returns `USER_CONFIRMATION_REQUIRED`. Only `buddystudy.voice.answer.submit` can freeze the edited answer into a private `VoiceTutorReviewedAnswer` binding. The native controller schedules the canonical operation through the existing serial MCP coordinator and requires the exact provider function-call acknowledgement before execution; provider arguments cannot supply the reviewed text. `McpVoiceTutorToolAdapter` rechecks owner/call/focus/revision and the exact bound canonical record. If source IDs are available, `canonicalAnswerTurns` verifies the complete original question/USER window and excludes those rows from post-call eligibility before the write. Typed-only answers are allowed; incomplete or oversized source windows mark private evidence incomplete instead of claiming provenance for the edited text. Original transcripts are never rewritten.
- Canonical record preflight and exact reviewed-answer readback recover an already accepted submission without overwriting or replaying it. Successful submission publishes `submitted` and permits the normal tool continuation to poll the canonical grading correlation. Grading uses only the saved result for the reviewed answer; raw microphone ASR and model-written feedback cannot substitute for it. Canonical tool data is bounded internally and compacted before model output; oversized answer fields are omitted explicitly rather than truncated. These fences do not establish acoustic accuracy of the original ASR.
- Trusted `buddystudy.voice.question.changed` carries only an exact positive study ID and canonical record ID. iOS reauthorizes an exact record GET, checks both IDs and QUESTION type, and fences overlapping account, call-attempt, record and study-room requests before reconciling cached state. Exact owner reads include SKIPPED records so a completed skip can be reconciled; browse and pending pages still hide them. Changed records are queried again on call completion. Existing local drafts and the user's active question choice are preserved; arrival of a different pending question or grade does not navigate or replace that selection. These refreshes use the existing record and study-room state paths.
- Raw `create_root_study`, `create_study_topic`, `update_study` and `delete_study` are not in the native catalog and are rejected if invoked through that path. `prepare_voice_study_mutation` normalizes and validates only the selected action's relevant fields, reads an exact owned target/parent and, for deletion, a complete bounded subtree. It stores one immutable pending proposal per call, bounded to 256 process-wide entries and 120 seconds, with an opaque ID, owner/device/authentication session/provider call, current lesson revision, source learner row, preparation response generation, exact patch and deletion membership. Preparation has no write side effect.
- The realtime model asks the returned natural question once and interprets the subsequent yes/no. `confirm_voice_study_mutation` accepts only proposal ID and boolean decision, never replacement fields. Execution requires the same scoped live call/revision, a newer durably persisted USER after the exact preceding completed tutor question, and server-observed learner speech starting after that question's actual playout stop. Exact target identity is re-read; update sends the frozen name/level/parent baseline into the canonical owner-locked CAS, and deletion sends the frozen subtree IDs into the canonical membership check. The pending ticket is consumed before a write may suspend; uncertain writes are never automatically replayed. A refused, expired, replaced or already-consumed proposal cannot mutate data.
- The live controller freezes the source USER item/epoch and preceding tutor item/playout order separately from the tool response's current lesson epoch. `initialLessonRevision` in each native tool context is the current execution epoch; `latestAcceptedLearnerLessonRevision` remains the original source epoch. This permits a successful change followed by exact selection on the same learner turn without rewriting history or requiring consent again. Current speech/response fences reject superseded work.
- Native persistence checks deliberately choose the latest USER, not the latest row of any role: a model's short pre-tool acknowledgement may already be stored. `persistedLearnerTurnId` and the native focus transaction ignore subsequent tutor preambles but reject every newer USER, even in another epoch. `persistedDialogueBoundary` additionally verifies the exact question before the yes, rejects an intervening tutor question, and allows a post-yes acknowledgement. Tools can retry the internal `INPUT_PERSISTENCE_PENDING` condition briefly; normal audio replies never use this durability gate, and the learner is not told to repeat a command.
- New/changed topics are selected through the same model-chosen structural focus method after a successful write when the learner already wants to study them. There is no legacy `AUTO_FOCUS_PENDING` classifier pipeline in this runtime. Off-focus edits preserve the selected lesson; updates apply only to new question epochs, and deleting a selected node clears that focus without deleting historical evidence or ending the call. The spoken policy remains concise, tree/level-aware, and does not expose tool/server mechanics.

### Shared storage/lifecycle and legacy transport details

The shared media, privacy, quota and persistence invariants below remain applicable. Any following description of independent live GPT assessments, rejected-input deletion, approved-transcript response gates, semantic question/feedback tags, no-interruption response gates or controller-minted mutation/focus leases is specifically the legacy implementation, not the active native conversation algorithm above. The native interruption change does not alter the PCM fallback.

- `VoiceTutorSessionView` retains call ownership and its existing foreground/exit lifecycle. A pure `VoiceTutorCallPresentation` maps connection phase, authoritative countdown/quota, pause state, and result state into the minimal `VoiceTutorCallScreen`; it cannot alter transport or allowance. Each start/retry omits `studyId`, retains only the explicit recording-consent snapshot, and uses an unconditional navigation destination, so reserving quota cannot remove an active call. Until a trusted focus arrives the topic remains the localized AI tutor label. Typed `buddystudy.voice.study.focused` metadata requires a positive exact ID, valid parent/title/level, and a newer positive epoch in the current attempt; stale metadata reads cannot overwrite it, and deletion fences resurrection. The ordinary call surface centers one tutor orb. Its tap delegates to the existing acknowledged `pause-v1` transition, upward/downward drags continuously reveal/hide the same persistent conversation, and an uninterrupted 2.2-second hold delegates once to the existing explicit end-call operation. Hold preparation at 0.35 seconds and the keep-holding instruction at 0.9 seconds are local presentation only. Release, movement, cancellation, disappearance, or attempt replacement invalidates the pending hold; after the 0.35-second preparation stage, that gesture cannot also fire the tap. A shorter stationary tap retains normal pause/resume behavior, while a drag owns disclosure only. A movement of at least 12 points permanently cancels that touch's hold, and one `DragGesture(minimumDistance: 0)` owns tap, swipe, and hold routing. VoiceOver's explicit End action uses a confirmation dialog; a completed touch hold invokes end directly. Transcript scrolling does not own orb gestures. State needed for connection, recording, pause, waiting, retry, or terminal recovery remains available with restrained visual emphasis and full accessibility. The call hides app tab/navigation chrome and preserves Reduce Motion, Dynamic Type, and VoiceOver actions for pause/resume, conversation disclosure, and explicit end.
- Voice Tutor is an authenticated iOS voice surface whose user-started calls continue through app backgrounding and device locking. The backend owns the OpenAI Realtime call, model instructions, API-key HMAC-pseudonymized safety identifier, credential, quota clock, provider failure mapping, and sideband. iOS receives no provider credential: it exchanges SDP through BuddyStudy, carries only RTP media on the resulting audio-only WebRTC peer connection, and sends local speech boundaries and lifecycle controls through BuddyStudy's authenticated control socket.
- iOS snapshots and validates its bounded audio-only SDP before installing the local description, then waits at most 15 seconds for the peer's combined ICE/DTLS connected state before opening control. The GA call configuration and sideband `session.update` both set `audio.input.turn_detection` to explicit JSON `null`. Within the existing connection timeout, `session.updated` must confirm that same nested explicit null; a missing field, legacy schema, VAD with false flags, HTTP upgrade, or `session.created` alone is not policy readiness. Live synthetic tests found provider VAD clearing WebRTC output even after both false flags were acknowledged. Only after disabled detection is confirmed does the backend send ready and request exactly one opening response through the same response/playout gate as subsequent turns. Trusted session instructions and a response-scoped override make that opening one direct topic-discovery question in the configured language, without a greeting, self-identification, role description, lead-in, or readiness question; the same override survives one provider-local retry. Learner agreement is still required before starting saved-topic instruction. The PCM fallback retains its existing provider VAD and begins its opening only after its configuration acknowledgement.
- `GET /api/v1/voice-tutor/status` is the compact authority for effective tier, availability, current monthly quota, reset time, the configured per-session ceiling, and optional-recording availability. `POST /api/v1/voice-tutor/sessions` derives the actual reservation from the current quota and creates one owned session; reusing its idempotency key is accepted only when study, language, model, voice, and recording-consent presence/version match the original request, while a changed operational duration ceiling or consent timestamp does not create a conflict. `POST /sessions/{id}/webrtc` negotiates the media call; `POST /sessions/{id}/end` finalizes it idempotently; and the paginated list/detail APIs return transcript, result, and safe recording metadata.
- `GET /api/v1/voice-tutor/voices/{voice}/preview?language=ko|en|ja` is a controller-facing preview boundary, separate from call creation and quota reservation. `default` resolves the configured Realtime voice; every other path value must exactly match the shared ten-voice allowlist. The service checks an authenticated registered principal, `VOICE_TUTOR_READ`, enabled server configuration and effective `planEligible`, but does not require positive remaining seconds and never reserves or charges voice-call seconds; normal entitlement/quota reconciliation may still materialize or roll over the user's quota projection. It sends one fixed localized phrase to OpenAI `/v1/audio/speech` with `gpt-4o-mini-tts`, the selected voice, MP3 output and the existing regular user-content key. Responses are nonempty `audio/mpeg`, bounded to 512 KiB, returned with `no-store`/`nosniff`, and neither backend preview audio nor fixed preview text is persisted or logged. A process-local admission policy bounds each user to one request per two seconds, eight concurrent provider requests and 4,096 inactive-user metadata entries; release is idempotent. iOS fetches raw data through the authenticated backend client, enforces the same byte ceiling, keeps only bounded in-process sample reuse, plays one sample through `AVAudioPlayer`, and never creates a voice session or changes an active call.
- `/api/v1/voice-tutor/sessions/{id}/webrtc` accepts one bounded audio-only SDP offer from the owner, creates the provider call with the server key, and stores the validated opaque call ID. `/control` accepts only that owner and `buddystudy.voice.control.v2` after negotiation, atomically permits one control claim, and attaches the backend to the same call over OpenAI's sideband. Both requests require `X-Voice-Turn-Protocol: realtime-native-v1`: missing/unsupported capability returns SDP HTTP 426 before provider allocation or closes control with protocol error before claiming the call. This prevents a server-only upgrade from opening a manual-turn session for an incapable client. The legacy `/stream` plus `buddystudy.voice.v1` PCM relay remains a fallback. A server-owned two-second pulse revalidates user/device authentication and renews the short lease; client heartbeats are validated, rate-coalesced advisory acknowledgements. Revocation, deadline, provider failure, or either transport's terminal close settles usage exactly once and initiates provider hangup.
- HTTP 101 confirms only the control upgrade, not continued media or sideband health. An upstream receive/send completion without an explicit local terminal signal is a failed call, even when its WebSocket close code is 1000. Bounded lifecycle diagnostics record the first terminal branch, numeric close code when observed, elapsed time, error class, and allowlisted event-type counts. Control logs correlate the owned session UUID with a truncated SHA-256 call reference; raw provider call IDs, close reason strings, exception messages, media, transcripts, SDP, and credentials are excluded. Close-status observation is passive and cannot delay teardown. See [OpenAI's sideband connection model](https://developers.openai.com/api/docs/guides/realtime-server-controls).
- WebRTC continuously renders both digital silence and nonzero comfort/concealment noise after speech ends. PCM activity cannot establish a normal response boundary: waiting for it to stop deadlocks subsequent replies, while a loudness threshold can mistake a quiet sentence tail for silence. For an uninterrupted response, `VoiceTutorWebRTCResponseState` therefore ends the UI's server-streaming state on the same response's generation-completed and output-buffer-stopped signals, in either order, without resetting the continuous RTP track. Native learner interruption uses the separate response-ID fence and immediate local output suppression described above. For the distinct server-verified `USER_ENDED` lifecycle only, iOS binds those two signals to the exact response generation and keeps the native output path alive for a bounded local tail fence before closing WebRTC. If terminal ownership races ahead of forwarding the second raw provider boundary, that lifecycle's server guarantee seals the still-active exact response ID locally instead of skipping the fence. The fence keeps a hard 1.25-second network-tail window anchored to provider stop even when continuous comfort noise produces an immediate renderer callback, then preserves the measured active `AVAudioSession` output latency, I/O buffer duration and largest bounded renderer quantum observed inside that window before teardown. Neither early nor later comfort noise can shorten or extend the hard network window; a newer response, transport close, task cancellation or explicit dismissal still invalidates the token; backgrounding alone preserves the final audio tail. Normal response progression never waits on this fence, and completion of the orb termination hold or its explicit accessibility end action retains the same server settlement/summary flow without entering the spoken-farewell wait because the explicit client-end contract cancels the active provider response. LiveKit/WebRTC exposes no response-scoped speaker-drained signal, and its receiver statistics are continuous-track aggregates, so this remains a defensive bounded drain rather than proof that the final sample physically left the speaker; packets arriving outside the bounded window cannot be attributed safely. The speaking indicator still requires a nonzero local render while that response's server output is active; post-stop noise cannot restart it or extend the bounded tail forever. Bounded iOS diagnostics use the existing app-log store for transport/device states, frame counts, route port types, a zero-volume flag, safe error kind/code, and first-stop source; they never retain PCM, transcripts, device names/UIDs, or raw errors.
- A successful OpenAI WebRTC negotiation is followed immediately, in a non-cancellable section, by an FK-free `voice_tutor_webrtc_cleanup_outbox` marker before the call is attached to `voice_tutor_sessions`. The session attach and marker's attached state commit atomically. A process can still be killed in the irreducible interval after OpenAI returns the call ID but before the first MySQL write begins; outside that micro-gap, attach failure and process exit remain durable cleanup work. If the first marker write and immediate hangup both fail, cleanup retries the marker write once in the same non-cancellable failure path and records an operational error if storage is still unavailable. Unattached markers are claimed with a bounded lease, while an attached marker is protected as long as its matching session provider ID exists. Account deletion removes that session row but not the FK-free marker, making the call claimable again instead of permanently orphaning it.
- `provider_session_id` remains the terminal-session hangup marker. Normal control cleanup and stale-session recovery clear an `rtc_` provider ID, and remove its cleanup marker, only after OpenAI confirms hangup or reports the call already ended. A failed hangup leaves both identifiers retryable; the session recovery scheduler scans bounded terminal and orphan-marker batches on every run. Legacy non-WebRTC provider IDs are neither selected nor cleared by this flow. Session quota settlement and learning-result finalization remain idempotent independently of repeated provider hangup attempts.
- Audio capture remains full duplex while tutor audio plays. The always-installed capture-post-processing tap copies normalized native WebRTC FloatS16 samples into one bounded serial acoustic pipeline without storing audio or requiring recording consent. The app bundles a pinned MIT Silero v6 Core ML model (~0.9 MB); a local-only, five-second model/warm-up preflight runs before reserving a session. A continuous AVAudioConverter resamples to 16 kHz; each 512-sample window retains the model's 64-sample context and recurrent state. Speech probabilities drive 80 ms onset / 1,280 ms quiet-release hysteresis. RMS only selects a native channel, never classifies speech or meaning. Readiness, explicit mute, teardown and capture-format generations fence the pipeline; tutor speaking never gates it. Queue, packet-age and prediction deadlines fail explicitly rather than reverting to energy detection or leaving a silent call. A hesitation can correctly be acoustic speech: meaning is a separate backend decision. See the bundled [model provenance and license](../StudyMate/Resources/SileroVAD/README.md).
- Each acoustic utterance emits `buddystudy.voice.input.speech.started` and `.stopped` with one positive integral `sequence`; later utterances increase it. A serial bounded stream preserves paired order. Attempt, account, socket and native-format generations reject stale work. Muting finishes an active utterance once; teardown cancels the old stream. For native conversations, a fresh speech-start edge also immediately suppresses the current ordinary tutor output and requests server-owned interruption through that same numbered event; capture, resampling and the peer connection remain active. Legacy no-interruption playback remains unchanged.
- `VoiceTutorAudioProcessingModuleFactory` explicitly installs capture/render delegates through their setters and verifies native getter identity before creating the peer factory. The bundled LiveKitWebRTC 144.7559.14 initializer ignores its delegate arguments; constructor injection alone silently leaves local speech detection uninitialized. The transport retains the SDK's weak delegates through teardown. Capture initialization and the first valid input frame each emit at most one metadata-only diagnostic, with counters/sample rate/gate also included in existing media snapshots. Optional recording normalizes copied FloatS16 samples to the recorder's Float32 range without modifying native media or enabling recording without consent.
- The control handler bounds and rate-limits these application-only events, rejects them before ready, and forwards only type/sequence to the server turn controller, never directly to OpenAI. A matching stop emits one reserved server `input_audio_buffer.commit`; duplicate/stale pairs and unmatched stops cannot commit. At most 32 utterances may await provider commit acknowledgement, recent provider item IDs are deduplicated, and a later utterance cannot extend the oldest acknowledgement's existing response-timeout deadline. New speech before an older ACK retains its own pending state. The backend exclusively owns `response.create`; the audio-only SDP prevents a modified client from bypassing that policy through a provider data channel.
- `VoiceTutorInputTurnCoordinator` correlates final ASR with exact acknowledged USER item IDs and the latest completed teacher sentence. An independent relay worker calls `VoiceTutorInputAssessmentUseCase`; it must never await GPT inside the ordered provider receive loop. The existing summary-model configuration (default `gpt-5.4`) and regular user-content key serve a strict-schema, per-item communicative decision plus orthogonal typed intents for lesson end, saved-topic selection and tree continuation. When the controller attaches an eligible immutable candidate offer, the assessment also receives the final tutor-audio transcript and must attest which candidate IDs the tutor actually proposed in that audio. Correlation accepts a nullable `targetStudyId` only when it is among those semantically attested spoken IDs and the server registry; merely appearing in candidate metadata, tool data or learner text is insufficient, and no offer means no target. There is no regex, filler-word list, minimum sentence length or transcript rewrite: short acknowledgements, negations, names, numbers, questions and partial ideas can all be meaningful. The termination prompt permits only the current speaker's direct, unambiguous, presently operative request to end the current voice lesson/call. Topic/task completion, questions, quotations, hypothetical/conditional/negative/future forms, silence, filler, tutor/tool content and ambiguous context fail closed. Input, teacher transcript, candidate labels and tool content are untrusted data, not instructions. The process-wide singleton admits at most four provider calls concurrently and lets at most sixteen additional assessments wait on the FIFO semaphore for 1.5 seconds; queue overflow or admission timeout returns `BUSY` without a provider retry. Once admitted, the default request deadline is ten seconds, including key lookup, HTTP and decoding. It is a maximum rather than a wait: ordinary turns finish after one model response, while an authorized root/child/update mutation uses a second, sequential independent semantic attestation within the same deadline.
- Spoken termination is emitted only after the exact final USER transcript is assessed meaningful and successfully persisted as a new turn. It is idempotent per acknowledged provider item, is never emitted for an internal long-speech checkpoint or failed persistence, and suppresses the normal `response.create`. WebRTC uses the authenticated client speech sequence; the legacy PCM path correlates the exact provider server-VAD start, stop and commit item ID to its own monotonic per-call sequence before entering the same semantic coordinator. Both controllers consume the server-only signal through the same `USER_ENDED` owner as the explicit end action, preserving ordered `buddystudy.voice.session.ended`, summary eligibility and quota settlement before transport close. Raw legacy transcript events cannot self-authorize termination without that correlated commit and assessment.
- Only approved, original final ASR is persisted and forwarded. A tutor reply waits for that persistence acknowledgement. Rejected USER audio items are deleted from the provider conversation by their exact IDs, and deletion acknowledgements gate subsequent default-context responses. No assistant item is deleted. Transcription/assessment failure is not classified as filler: it produces a compact nonterminal `buddystudy.voice.input.retry` hint and removes the unapproved input. Pending work is bounded (16 acknowledged items, 8 per batch, 4,000 characters per item / teacher context, 16,000 total batch characters); ASR has an eight-second deadline and publishing/deletion has a five-second deadline. Malformed decisions, duplicate IDs and late/closed generations cannot approve a turn. Cleanup failure closes the controller immediately and reaches the relay through a failure signal independent of outbound socket backpressure.
- Legacy `local-vad-v1`/PCM turn gating only: each trusted tutor response is one short, complete sentence. The next legacy WebRTC response requires that same response's `status=completed` and `output_audio_buffer.stopped`, successful relay of that exact provider boundary, durable storage of any nonempty final tutor transcript, no unresolved input work, and at least one persisted meaningful learner item after its acoustic stop. Learner assessment release, the next response, spoken lesson end and checkpoint resumption remain behind the exact post-relay storage acknowledgement; an empty final transcript is a valid transcript-free spoken boundary. The receiver/track plays consecutive RTP samples without a boundary reset; the old `buddystudy.voice.playout.drained` event cannot open or block a turn. An exact response-local clear, cancelled/incomplete/failed response, or correlated provider request error receives at most one clean retry. If that retry fails, only the matching response is abandoned and the learner is asked to repeat while the RTP and control call stay connected. An accepted MCP side effect is never replayed; malformed or ambiguous session, input, tool and provider-boundary failures remain terminal. The PCM fallback retains provider VAD, playback acknowledgement and conservative floor. For long ongoing learner speech, the configurable timer repeatedly commits internal ASR checkpoints without stopping capture. Checkpoint commit/assessment/publication backpressure defers only one checkpoint; it never schedules an intervention or bypasses `userSpeaking`. Only the normal stopped-turn gate creates responses, including after MCP work. An immediate final stop waits up to 250 ms for a nonempty tail; rapid mute/unmute stops in that interval share one commit and settle all reserved speech slots. Response watchdog and explicit termination may cancel a broken or intentionally ending response, never merely because the learner began speaking or spoke for a long time.
- `user_membership_tiers.monthly_voice_seconds_limit` owns the plan allowance. Defaults are zero for TIER1 and 3,600 seconds for both TIER2 and TIER3. The existing membership-tier administration boundary can change `monthlyVoiceSecondsLimit` without an app release, and the per-user administration boundary can set or clear an independent monthly override. The exact override sentinel `31,536,000` seconds is serialized numerically by the backend but rendered as effectively unlimited by iOS; it never changes the provider/session ceiling. `VOICE_TUTOR_MAX_SESSION_SECONDS` is a separate operational ceiling, defaults to 3,600 seconds, and is hard-clamped to the provider's 60-minute session limit. A new reservation is at most `min(remaining_seconds, max_session_seconds)`, so the default 60 monthly minutes may be used in one call or split across shorter calls.
- `user_voice_quota` is independent from question `user_quota`. It owns a fixed account-created anchor plus voice base, optional per-user limit override, used, reserved, remaining, and current-period seconds; it does not adopt the question quota's optional first-purchase anchor change. Voice status, history/detail, and session-start access first reconcile any expired session and lazily advance an overdue voice period. Session start then reserves atomically and `voice_tutor_sessions.monthly_quota_exhausts_at_hard_end` freezes whether that accepted reservation owns all finite remaining monthly capacity at its hard end. Terminal settlement charges rounded-up backend-observed connected seconds (or the greater accepted-PCM duration on the fallback), then releases the unused reservation. Duplicate end, disconnect, timeout, and recovery work converge on the same session identity without negative remaining time.
- Mid-call monthly exhaustion uses a terminal response boundary distinct from ordinary turn progression and from `TIME_LIMIT`. Once current quota reconciliation confirms that the frozen session actually reaches zero, the backend atomically owns `QUOTA_EXHAUSTED`, closes learner input while leaving remote audio available, and creates one short localized notice tagged with the private provider metadata marker. The sanitized control event exposes only an exact boolean marker bound to the provider response ID. Cleanup and settlement require that same response's `response.done`, `output_audio_buffer.stopped`, and matching iOS `buddystudy.voice.playout.drained` acknowledgement; early, stale, unmarked, or wrong-response acknowledgements cannot release the boundary. A control-socket close does not prematurely cut the bounded direct-media drain, and retries/recovery retain the quota terminal reason. If the frozen monthly boundary exactly collides with the provider's 3,600-second cap, the server may pre-arm the notice eight seconds before hard end; that notice uses the final reserved service seconds, iOS keeps their countdown visible, and settlement occurs no earlier than the exact hard boundary. The response has a three-second pre-audio watchdog so a silent generation can be cancelled and retried once inside the lead window; observing output start or audio restores the ordinary response deadline and forbids regeneration so spoken audio is never cut by that watchdog. A provider/per-call 60-minute end that does not exhaust monthly capacity remains `TIME_LIMIT`, emits no monthly-exhaustion claim, and does not use the marked quota-notice acknowledgement as evidence.
- `voice_tutor_sessions` owns session lifecycle, topic context, duration, consent audit fields, and timestamps; `voice_tutor_transcript_turns` stores bounded ordered text turns; `voice_tutor_results` owns the one-to-one private learning result; and `voice_tutor_recordings` stores one private object's integrity/status/retention metadata without the binary. FK-free `voice_tutor_recording_prefix_cleanups` rows deliberately survive account deletion so the deterministic S3 owner prefix remains retryable. The full session detail remains private source evidence. Validated node-linked exchanges are canonical `VOICE_TUTOR` records with the same ID/publication/comment paths as `QUESTION`, not separate posts.
- `VoiceTutorStudyContextPort` freezes owned lesson metadata in `voice_tutor_study_snapshots` (V102). Topicless discovery prepares no invented node, title or level. Legacy requests with a numeric `studyId` still prepare that accepted topic and the first ten owned direct children, but current iOS always omits the field and exposes no selected-study construction path. Read-only MCP browsing never captures new snapshots, even after selection; existing frozen metadata may enrich reads. Explicit `select_voice_study` and one-edge `advance_voice_study` focus changes capture their full verified parent paths; explicit child creation/update retain their bounded capture paths. A transactional session-row lock enforces a 64-node session limit and immutable first-seen values. Terminal/expired sessions cannot append snapshots. Only the session has a foreign key, so study deletion cannot erase historical names/levels; session/account deletion still cascades the private metadata. Failed or cancelled preparation attempts settle the unconnected reservation, with existing lease recovery as the bounded-cleanup fallback.
- V104 retains those baselines and adds at most 32 append-only `voice_tutor_study_revisions`, shared by explicit focus changes and metadata revisions. `update_study` first captures the old node, saves the partial settings update, then captures its new owned metadata; failure after the write reports saved settings with unprepared lesson context, never a false failed write or automatic replay. Each server-created tutor response and learner input has a fixed `lesson_revision`, carried only in internal transcript metadata. New questions resolve that revision; old pending questions keep their original level despite late ASR or later answers. Unknown `-1`, nonexistent epochs and ambiguous metadata cannot become node-linked evidence. `accepted_study_id` remains the immutable original create request (null for discovery), separate from `study_id`'s current live `ON DELETE SET NULL` focus. Repeated creation uses that original value for idempotency, not the focus subsequently chosen by speech.
- Additive V106 introduces session-owned `voice_tutor_lesson_focuses`: `(session_id, revision)` identifies each confirmed study focus, with a historical study ID that survives node deletion. Only known legacy accepted IDs are backfilled at epoch zero. `VoiceTutorLessonFocusPort.focus` verifies an active owned session and the complete live/frozen owned parent path, then captures metadata, appends a shared lesson revision plus focus row, and changes the session's live title/level/pointer in one transaction. Same-focus retries retain the current epoch rather than creating duplicate revisions or rolling back a later level edit. Selection consumes neither question quota nor a question/answer mutation. Summary evidence and the canonical projector resolve the focus at the question's epoch; epoch-zero discovery and cross-focus claims cannot be filed under a later selected node. Deletion retains historical evidence but disables new lesson questions on that node; a separately verified surviving choice can resume the conversation.
- Additive V107 stores the accepted learner-turn identity that authorized each non-legacy focus epoch. `(session_id, learner_turn_id)` is unique when present, so one accepted utterance cannot select or advance through multiple nodes. Every bounded candidate page is validated against its exact owner-scoped query or parent, limit, offset and stable total. An offset-zero page with `totalCount > 1` proves a real branch immediately and contributes only actual nodes from the stable contiguous prefix, capped at 16; a later node requires a narrower exact read and fresh spoken choice. Only an exact offset-zero page with total zero or one proves a leaf or single-child edge. Nonzero, gapped, duplicate, truncated, inconsistent and unscoped output cannot create that authority. The controller builds a bounded call-local graph, follows only proven single-child edges to a leaf or first real branch, and carries separate per-candidate topology evidence for every offer. It promotes an offer only after a later audio response supplies one exact final tutor transcript item and reaches its matching output-buffer stop. The semantic assessor must attest from that final audio transcript which registry candidates were actually spoken. Only after the next exact final non-checkpoint USER item is assessed and persisted does the controller bind one spoken target, typed selection/continuation intent, opaque provider item, lesson revision and tutor speech boundary in the server-only dialogue boundary. Storage failure, candidate mismatch, a newer learner/tool generation and reconnect expose no focus authority. Selection and advancement require the tool's numeric argument to equal that attested target. For advancement the controller freezes the exact tutor-question, semantic learner-answer, tutor-feedback, navigation-offer and learner-continuation provider item IDs. Feedback and the navigation offer may share one response or be two consecutive spoken responses; an intervening unrelated or unanswered tutor turn invalidates the exchange. The confirmation adapter verifies those exact persisted rows, roles and order in the active revision, while acoustic proof shows the relevant tutor output stopped before continuation speech began. The focus transaction locks the exact live device authorization before the matching active provider call, then locks and rechecks the current revision, focus path, offered metadata, every proved single-child/leaf range and direct-parent edge. Concurrent logout, rename, reparent, deletion, sibling insertion and endpoint-child insertion therefore serialize before or after the accepted focus decision. The attestation is consumed once, and the database unique learner-turn fence remains the durable second barrier. The realtime model cannot supply target, intent, provider identity, revision, exchange identity, traversal evidence or speech boundaries through tool arguments.
- The trusted spoken lesson policy uses actual saved study IDs, parent links and each node's frozen 1–10 difficulty. `VoiceTutorLessonTreeContext` supplies bounded ancestor paths and explicit selected/descendant/same-tree/other-tree/unresolved relations to initial instructions and successful MCP study outputs; incomplete paths remain unknown rather than inventing a root. Requested nodes have priority before owner-checked ancestors within the existing 64-node cap. An ordinary first topic-bearing learner utterance is discovery only. Discovery follows complete single-child pages without selecting intermediate nodes, then speaks the verified endpoint and waits for a new confirmation; at a true branch it asks one short choice among at most three actual returned children. The explicit new-root exception requires the same current persisted turn to pass both semantic decisions for exact creation and immediate start, followed by the server-owned exact readback and `CREATED_ROOT_IMMEDIATE_START` focus result. The equivalent update-and-start exception requires both semantic decisions to approve the exact saved-node patch and immediate lesson start. The bridge retains the accepted learner item's source revision separately from the exact prepared post-update revision, returns `AUTO_FOCUS_PENDING`, and schedules one bound `select_voice_study`; only `UPDATED_STUDY_IMMEDIATE_START` at that fenced revision can open the first question. A newer meaningful learner turn invalidates the older start lease, and any update/readback/focus mismatch fails closed without replaying a committed patch or using stale name/level metadata. A successful focus tool or explicit continue decision grants one server-owned question purpose. It is consumed only by a clean, complete, single-item tutor-audio response whose exact persisted transcript is independently classified as one substantive question; tool/audio mixtures, setup or navigation speech, multiple questions, refusals, errors, timeout and overlong transcript fail closed. One continuous learner answer can include up to 32 semantically answer-bearing persisted parts. Earlier checkpoints remain unlinked until the final event presents the exact ordered provider IDs, at which point persistence atomically inserts the final row and promotes only those same-session, same-revision USER rows with no intervening tutor. Filler-only tails remain unlinked; missing, duplicate, reordered or partial sets cannot be summarized or graded. After that answer and fully spoken supported 0–100 feedback, the tutor waits, reads the current node's real children, speaks the proposed direct child and accepts only a fresh target-bound continuation before advancing exactly one edge. This is tree progression, not automatic harder why/how/counterexample generation. Greetings, readiness, silence, filler, hints and learner follow-up questions do not authorize grading, new nodes, focus changes or level changes.
- Summary contract `voice-tutor-summary-v2` adds `explorations_json` (nullable for older rows, V101) and a backward-compatible `result.explorations` array. Before the single summary-model call, `VoiceTutorSummaryTranscriptEvidence` projects the durable transcript down to at least one server-authorized `is_study_question` TUTOR row, every ordered USER row linked to that question, and only exact feedback linked to the final answer row. Setup, discovery, root/topic mutation, selection, consent, settings and unrelated transcript rows never enter even the global summary prose. Character limits are applied only between complete connected exchange groups, so a multipart answer or its feedback is either wholly present or wholly omitted. The model may extract topic/level/depth, `TUTOR_QUESTION` or supplemental `LEARNER_QUESTION` exchanges, optional spoken score, supported feedback, and exact transcript turn IDs. `LEARNER_QUESTION` evidence cannot make an otherwise setup-only call eligible. Server checks require a tutor-question extraction to cite the exact complete ordered durable answer set; prefix, suffix, duplicate and reordered subsets are rejected. Feedback can support a score/strength/improvement only when it cites the single clean TUTOR item whose `study_answer_turn_id` targets the final linked answer in the same revision, with no later linked answer or intervening tutor. Invalid exchanges are dropped without discarding a valid overall summary, and unsupported numeric scores are cleared instead of regraded. Only matching versioned session snapshots establish saved study identity/difficulty. Provider JSON is bounded to 12 topic groups, 12 exchanges per group, 48 exchanges total and 256 KiB. Verified groups can split by a question's revision into at most 48 stored groups while retaining the same total-exchange/byte limits. Strict schemas, plain-text sanitization and native-safe map serialization apply before persistence. Summary extraction is not proof of model assessment accuracy or independent mastery measurement.
- V105 supersedes V103's separate private record store: `questions` owns both record types, common text, canonical ID, ownership, visibility, engagement and soft deletion. `voice_record_id` uniquely references `voice_study_learning_records`, now only the immutable typed extension (kind, frozen node and transcript references, strengths, gaps, depth and translation hashes). Common text is moved, not dual-written, and old extension IDs/translation IDs remain stable. The source-backed projector still checks role/order, explicit-score provenance, owned frozen tree and all existing size limits. Extension insertion, canonical `QuestionPort.save` with search refresh, at most 48 exchanges, translations and result completion share the session-first transaction. Recovery is idempotent and never resurrects a common soft-deleted record. Historical rows and sessions predating migration are private, including late summaries; new sessions snapshot the normal record sharing default, still gated by live account sharing. Old binaries must be stopped before V105: removed duplicate columns require a database backup for schema rollback, not a JAR-only rollback.
- The existing `ContentTranslationRequestedEvent` and configured `LOCALIZATION_CONTENT_TRANSLATION_REQUESTED` stream carry `VOICE_STUDY_RECORD`, not a new stream or worker stack; existing stream names/topology remain unchanged. Only question/answer/feedback/depth/strength/improvement text is translated; ko/en/ja projections retain source hash, request token and PENDING/READY/FAILED state. Hash/token/status/record-existence CAS rejects deleted or superseded work. Read repair is durable and deduplicated with a five-minute retry cooldown; missing/failed translation displays original content. `PrivateLearningContentLogScope` follows coroutine children and blocking provider calls, suppressing private request/response history and provider-error text; retry causes are sanitized because the shared stream dispatcher also logs them. Voice content is also excluded from node-history HTTP and iOS diagnostic body logging.
- `GET /api/v1/studies/{studyId}/learning-records?scope=node|subtree&limit=30&cursor=...&tl=...&view=localized|original` selects both types from the canonical record store and returns the same `record` used in Records/detail/public comments. Exact node IDs and owner checks precede bounded subtree paging; no topic-name matching occurs. Legacy source/evidence-ID keysets remain compatible, owner/node/scope-bound, and advance past deleted hydration rows. `voiceRecord.recordId` links legacy evidence to the canonical ID; its existing `id` and owner-only `/api/v1/voice-tutor/learning-records/{id}` stay compatible. iOS retains its existing SettingsStore record cache and bounded node pages. The additive `StudyRecord.recordType` distinguishes read-only voice exchanges from generated questions; only QUESTION can enter pending/draft/grading/schedule or text-based legacy identity fallback. Canonical ID alone identifies voice records across all surfaces.
- Optional `pause-v1` runs over the same bounded control FIFO as numbered speech: pause request, final capture-stop edge, input-quiesced fence, same-response output completion plus pending input/MCP acknowledgement, server input clear and its acknowledgement, then `pause.state`. Resume keeps capture closed until a fresh clear is acknowledged, then reopens listening; there is no separate user-facing mute preference or state to restore. Only one raw-buffer clear may be outstanding, with a five-second ACK deadline; held silent RTP is cleared every 30 seconds. Transitions and stale ACKs are bounded/idempotent, and timeout ends with explicit failure and normal settlement. The pause transition itself introduces no output clear, response cancellation, truncation or legacy iOS playout-drain gate; native learner interruption is a separate path. Clock, heartbeat and explicit call lifecycle remain authoritative through foreground and background execution.
- iOS keeps the new result inside its existing server-backed detail cache. Collapsed topic/exchange sections reveal four additional entries at a time; source transcript display is lazy and paged by 20. Scores are shown only for answered tutor questions, never as a default zero. `StudySettings.voiceTutorVoice` is a local `SettingsStore` preference, preserved by settings reconstruction and backend refresh. Its supported Realtime name is captured before the first session-creation await; retries retain both that name and the idempotency key. Default omits the request field so `OPENAI_REALTIME_VOICE` remains authoritative. Voice changes affect the next session only and never touch a live peer, recording consent, quota or answer drafts.
- `StudySettings.voiceTutorLanguage` uses that same local preference boundary, with `default` resolving to the app language and explicit `ko`/`en`/`ja` choices. Missing or malformed stored values fall back without discarding other settings. AppState freezes the resolved language alongside voice before registration or token recovery, and backend settings refresh/copy operations preserve the local selection. Preview requests use the draft call language and discard cached samples when it changes. The backend persists the existing validated session `language` and configures input ASR separately. `VoiceTutorLanguagePolicy` supplies the selected spoken language in both complete session instructions and every native/legacy special response override, because provider `response.instructions` replaces session instructions. Ordinary responses continue inheriting the complete session policy. No new provider language field, database column or API contract is introduced.
- Voice lifecycle, metering and post-call summary do not themselves call question generation or consume question allowance. The active native lesson's explicit canonical question operations use the ordinary generation, answer-submission and grading use cases described above. Only the learner's explicit review/edit/submit flow reads or updates that canonical question's local draft. Question/result synchronization preserves record-ID-keyed drafts and the user's active question selection.
- Realtime frames are never persisted as database BLOBs or copied into API/external-provider logs, analytics, Sentry, MCP, or backups. Optional call recording is independently default-off and requires fixed-version consent captured on the session before any upload grant exists. iOS records both WebRTC processing taps, aligns them by monotonic time, mixes them at bounded gain into one AAC/M4A file protected from backup, then uploads directly with a short signed PUT. Local files and manifests are owner-bound and generation-fenced: logout, authentication invalidation, account replacement, or withdrawal advances the fence and purges them, while an older in-flight export/save is rejected and cleaned instead of recreating data after the purge. Private S3 keeps the binary; MySQL keeps consent and integrity/retention metadata. Completion HEAD-validates content type, exact bytes, and SHA-256, then uses a PENDING-only upload-contract snapshot CAS before publishing or rejecting the object. Same-contract PENDING retries may renew a grant by CAS without extending the absolute call-end retention deadline; a different contract and FAILED/DELETED metadata cannot be reissued. Owner-only short signed GET fails closed at that deadline, while immediate all-version deletion and default 30-day call-end retention cleanup are the ordinary binary lifecycle paths. Delete and retention first claim the observed metadata snapshot as DELETED, then remove the claimed key, so a concurrent grant renewal cannot be deleted accidentally. Deleted keys are physically re-deleted minutely through the configured upload-completion safety deadline and daily forever afterward. Account withdrawal commits a permanent FK-free cleanup marker in an independent transaction before the initial prefix attempt and relational cleanup. Initial S3 or outer-transaction failure cannot roll back that marker, and S3 unavailability does not indefinitely retain the withdrawn relational account. The managed job deletes all prefix versions and delete markers minutely through the safety deadline and daily forever afterward; it never removes the tombstone. The private bucket must also expire current versions, noncurrent versions, and delete markers no later than configured recording retention. Object keys, bucket names, and presigned URLs never enter logs or MCP.
- Realtime behavior is configured by `VOICE_TUTOR_ENABLED`, `OPENAI_REALTIME_MODEL`, `OPENAI_REALTIME_VOICE`, `VOICE_TUTOR_MAX_SESSION_SECONDS`, `VOICE_TUTOR_CONNECT_TIMEOUT_SECONDS`, `VOICE_TUTOR_RESPONSE_TIMEOUT_SECONDS` (default 60, runtime-clamped to 10–120), `VOICE_TUTOR_HEARTBEAT_LEASE_SECONDS`, `VOICE_TUTOR_CONTINUOUS_SPEECH_INTERVENTION_SECONDS` (default 12, runtime-clamped to 5–30), result-recovery settings, `VOICE_TUTOR_SUMMARY_MODEL`, `VOICE_TUTOR_SUMMARY_PROMPT_VERSION`, bounded summary attempt/backoff settings, `VOICE_TUTOR_TRANSCRIPT_MAX_CHARS`, optional `VOICE_TUTOR_PUBLIC_BASE_URL`, and the independently gated recording/S3/retention settings documented in `backend/README.md`. A claimed eligible summary remains `PROCESSING` through capped summary-generation retries and becomes `FAILED` only after exhaustion; cancellation or result-write failure still uses processing-lease recovery. V108 is an expand migration for start-first rollout: token-aware `PROCESSING` claims carry an opaque 36-character UUID, while pre-migration and overlapping legacy `PROCESSING` rows may retain NULL; any non-NULL token is forbidden outside `PROCESSING`. A stale token-aware re-claim replaces the UUID. Claimed completion/failure compare-and-set both the token and exact persisted `DATETIME(6)` claim timestamp before changing either result or session status, so an expired worker cannot finish or fail the replacement claim even when an overlapping old binary renews only `updated_at`. A legacy NULL owner may finish under the old contract until it is reclaimed. After old writers are retired, a later contraction can backfill abandoned NULL processing rows and require tokens for all remaining `PROCESSING` results. The call and sideband reuse the existing server-only regular user-content `OPENAI_API_KEY_USER`; no Voice Tutor-specific API key is accepted. Plan and per-user quota remain database/admin-owned rather than environment-owned.
- Additive V109–V113 make transcript-row provenance, not transcript volume or model prose, the learning source of truth. V109 adds nullable `study_question_turn_id`, which links each accepted USER answer part to the exact TUTOR question row. V110 adds USER-only `asked_study_question` for supplemental learner-led study questions; it cannot independently open the summary gate. V111 adds database checks that a question-linked answer is a USER row and is mutually exclusive with learner-question attestation. V112 adds TUTOR-only `is_study_question`, the durable proof that a clean completed tutor item consumed a server-owned substantive-question purpose. V113 adds nullable TUTOR-only `study_answer_turn_id` for the exact feedback item linked to the final answer row and forbids that feedback row from also being a study question. The transactional persistence adapter additionally rechecks owner/session, focus, exact role/order, same lesson revision, complete multipart membership, absence of an intervening tutor, the last linked answer, and feedback order; the schema columns alone do not grant authority. A finalized call without at least one V112 question joined to a V109 answer synchronously completes a blank technical result without invoking the summary model or projecting a canonical/public record; this empty-result claim cannot overwrite `PROCESSING` or `COMPLETED`. These additive migrations do not delete, rewrite, regrade or republish historical transcripts, results or records.
- MCP adds owner-scoped read-only `list_voice_tutor_sessions`, `get_voice_tutor_session`, and `get_voice_tutor_quota` tools and snapshot resources `buddystudy://voice-tutor/sessions/recent` and `buddystudy://voice-tutor/quota`. MCP reuses the application read use cases and may see safe recording status metadata in owned detail; it cannot open media/control transports or start, reconnect, end, extend, upload, download, or delete a session or recording.
- The legacy server-owned WebRTC sideband exposes twelve existing authenticated MCP tools as Realtime functions: `list_studies`, `get_study`, `create_root_study`, `create_study_topic`, `update_study`, `delete_study`, `list_records`, `get_record`, `list_study_learning_records`, `get_voice_learning_record`, `get_topic_stats`, and `get_study_growth`, plus voice-only `select_voice_study` and `advance_voice_study`. `McpVoiceTutorToolAdapter` implements `VoiceTutorMcpToolPort` through the existing permission-checked `McpTransportContext`; it neither creates another MCP server nor forwards the app bearer to OpenAI. SDP and `session.update` advertise identical effective function names/descriptions/schemas and automatic tool choice, with readiness also checking manual VAD. The captured principal is reauthorized before operations and after suspended reads. All four data mutations are server-owned and require one exact spoken proposal plus fresh independently verified agreement; provider function arguments cannot originate or change those writes. Root creation is owner-scoped/create-only, children require a verified owned parent, and updates patch only chosen metadata. `StudySyncUseCase.createRootStudy` and `updateStudy` share the owner-row mutation lock; normalized duplicate detection and metadata updates serialize. The partial update preserves schedule, model, key, activation and drafts; parent-scoped reads use exact owned-parent database pages/counts.
- Additive V114 adds an optimistic `version` to every study row. Metadata-only writes and scheduler claims increment it in SQL, while a voice update additionally compares the server-read parent, topic and level in the same conditional update. This prevents an ordinary settings or scheduler save from restoring an older name or level after a concurrent voice change. A scheduler recovery never reuses the mutable entity from a failed optimistic update; it reloads the latest owned row before recording only the new schedule failure state.
- The controller keeps at most 17 recently verified owner-read mutation candidates, seeded by the bounded initial owner snapshot and refreshed only by typed successful reads and validated mutation results. This cache is separate from `CandidateDiscoveryGraph` lesson traversal: editing/deleting a known root needs neither child-page completion nor lesson selection. `OWNER_READ` freezes those identities and the latest completed actual tutor transcript for semantic ASR-alias/coreference resolution, not authorization. Ordinary turns and failed USER writes do not erase previously verified read metadata. Each fresh mutation request still requires dual semantic attestation and then its separate exact proposal confirmation. Successful updates refresh the cached identity, validated child creation contributes its exact saved target, and deletions evict every deleted ID. An off-focus edit keeps the existing selected node's frozen name/level at the new metadata epoch; it never selects the edited node or rewrites historical question context.
- A failed persistence acknowledgement grants no mutation authority or learner continuation. Proposal execution is bound to the fresh confirmed USER item, not the original request item; stale, changed, rejected or spent proposal identities cannot revive permission. Bounded owner-read metadata can identify a later proposal but cannot supply consent.
- Mutation proposal extraction is separate from execution. A current final meaningful learner turn must express the operative root/child/update/delete request; at most three earlier durably acknowledged learner items may resolve omitted details. Pending, failed and co-batched items are not borrowed as persisted context. Both the structured input assessment and independent mutation attestation agree on the exact learner-owned fields, normalizing requested spoken levels semantically to 1–10 and applying creation default 5 only when absent. Current text remains raw; local validation checks bounded values and exact source provenance rather than classifying words.
- The controller retains the canonical attested request without minting a write lease, generates one no-tool confirmation question, and freezes `VoiceTutorStudyMutationProposal` with its opaque bounded proposal ID, action, owned target/parent, proposed name/level and optional compound start purpose. Only the actual completed tutor transcript after output drain enters the next eligible final input; a spoken intention to ask or an interrupted/unheard question is not consent evidence. Primary classification emits `CONFIRM_STUDY_MUTATION` or `REJECT_STUDY_MUTATION` with only that proposal ID, never replacement write fields. A separate verifier receives the fresh learner transcript and exact spoken proposal but not the primary verdict. Natural yes/no needs no repeated command; changed action/target/parent/name/level, unrelated or conditional speech cannot confirm the old proposal. Both agree, and the matching newest USER row must persist, before the controller dispatches the unchanged canonical action under a fresh account/device/call/revision-bound one-shot lease. Checkpoints, stale IDs, missing persistence, old approvals, suspended work and replay fail closed.
- Root creation is create-only under the owner mutation lock: a normalized owned root returns unchanged with `created=false`, a same-name child conflicts, and creation consumes no question quota. Exact returned-ID `get_study` readback and both output acknowledgements precede a spoken outcome. Missing/mismatched readback never retries an uncertain write. Create-only results require normal separate lesson selection. If the original request and confirmation both cover immediate learning, `AUTO_FOCUS_PENDING` drives only the server-owned readback/preparation and revision-fenced `select_voice_study`; `CREATED_ROOT_IMMEDIATE_START` or `UPDATED_STUDY_IMMEDIATE_START` alone grants the first question without another readiness prompt. Failed focus cannot reuse old permission, replay the write or teach stale metadata.
- Ordinary discovery uses exact owner-scoped name queries and parent-scoped child pages, follows verified single-child chains and speaks the actual endpoint or at most three real branch choices before fresh selection. Typed read metadata supplies identities, never permission. `select_voice_study` binds the latest persisted target choice to that actual completed offer and validates its full path. `advance_voice_study` additionally requires a completed verified question/semantic-answer/feedback/continuation sequence, current revision and one live direct-parent edge; siblings, ancestors, grandchildren and other roots cannot use it. Each target lease is consumed once under the focus transaction, typed successful focus results alone update the call, and function-output acknowledgements and normal speech gates still precede teaching.
- Voice deletion uses the same one-proposal/one-confirmation path as creation and updates. `DELETE_STUDY` supplies an exact owned target and learner evidence, never patch values or a lesson-start flag. The server first asks whether to delete that target and its descendants. Only a fresh independently verified and durably persisted confirmation releases a call-bound one-shot deletion lease; model-originated calls have none. The adapter re-reads and validates the entire subtree (maximum 128 nodes), checks frozen target and live call/revision, consumes the lease, and supplies `expected_study_ids` to the owner-locked atomic deletion. There is no client-visible confirmation token or repeated command. Ambiguous/changed scope, inactive calls and uncertain results cannot delete or automatically retry. Prior answers/session records remain, deleted targets cannot receive new questions, and deletion does not end the call.
- In the native conversation, a same-revision `get_grading_process` remains a read of a previously accepted answer when newer speech arrives. A superseded follow-up cannot relabel that answer as failed. Successful `prepare_voice_study_mutation` supplies a typed server confirmation question for the exact acknowledged tool continuation; the model reads it once with tools disabled, and an empty response receives at most one response retry without repeating the mutation tool. An exact latest empty-audio commit rejection settles its own client acoustic sequence when no reply remains; it cannot revive an older proposal, confirmation or tool continuation.
- `list_records`/`get_record` expose both canonical record types; `gradingResult` remains ordinary-question-only and `voiceRecord` carries supported spoken feedback with nullable score. Node-history tools still default to original text, five entries (maximum 30), a 512-character cursor, and a three-record initial tutor page. HTTP MCP reads remain owner-scoped; the voice bridge additionally proves a shared actual root, bounds ancestry reads and reauthorizes after suspension. `get_record` takes canonical `record.id`; the compatibility evidence tool still takes numeric `voiceRecord.id` and returns its canonical `recordId`. Results over 16 KiB fail explicitly. Prior records are never a current answer, new consent or a reason to regrade.
- In the legacy conversation path, `VoiceTutorMcpTurnCoordinator` accepts functions only from the matching successful `response.done`, registers call IDs before work, and bounds eight calls per response, seven tool rounds per learner turn and 256 IDs per call. Seven rounds cover one saved-root query plus the six dependent direct-child reads needed to prove a legal root-plus-five chain's endpoint is a leaf. A serial MCP worker runs outside provider receive with a 15-second operation deadline, bounded JSON arguments/results, no automatic replay, and an explicit uncertain-write result on timeout. Each `function_call_output` must receive its exact item/call acknowledgement within 15 seconds before `response.create` continues. A proven function-only response requires no nonexistent audio-stop event; a mixed spoken/tool response still waits for the same response's output buffer to stop. New learner speech/assessment/persistence gates continue to apply, with no audio cancellation, clearing or truncation. Opening responses cannot execute tools. Only verified successful mutations are eligible to request the live `buddystudy.voice.study.changed` refresh hint; the server-internal result owns the mutation kind and bounded deleted IDs, and the sanitized iOS wire payload contains only the event type, one positive `studyId`, that server-owned kind and the bounded deleted-ID list—not tool output, titles, levels or prompts. A normalized existing-root result has `created=false` and requests no false change hint. Delivery remains best-effort behind the live controller gate; when received, iOS fetches the exact node through the existing study store, and ordinary study sync remains authoritative. The hint never invokes the interactive deletion flow, changes the current answer draft, or deletes source records.
- Summary generation and connection termination are independent. The first control terminal decision atomically owns its source, reason and failure; a late socket EOF/error after `CLIENT_END` cannot turn a completed call into a failure, while a prior real provider error remains a failure. Finalized failed calls may still claim a pending result only when they already contain a durable server-authorized study question joined to its exact complete learner answer; transcript volume, setup questions and learner-led questions alone are not eligibility. Setup, study-tree mutation, consent, settings and topic-discovery turns complete with an empty technical result even when transcripts exist, without a summary-model call or canonical/public record. A narrow bounded recovery also recognizes legacy, unattempted failures whose result error exactly copied the call failure and has no model, but applies the same verified-exchange gate before invoking GPT; a terminal summary-generation failure after its configured in-claim attempts is not automatically reclaimed. Cancellation/persistence failure retains the existing processing lease for recovery, with V108's token-and-claim-time fence invalidating every older worker after re-claim. `VoiceTutorSummaryState` distinguishes pending, ready, empty and failed; a shared identity-fenced poller allows at most eight reads with 250–5,000 ms delay, offers a read-only recheck on delay/read failure, and refreshes cached history on every entry.

- Consented live recordings retain their open-file protection while locked. Final export, hashing and manifest save wait for protected-data availability and retry access-denied failures only when a lock transition is observed; originals remain until manifest success. Each attempt revalidates the owner generation. Ordinary corruption, missing files and owner invalidation still follow the existing cleanup path.
- iOS runs owner-independent recording cleanup on app startup and every foreground entry, before onboarding or maintenance guards. Valid pending recordings expire after 30 days and orphaned track/manifest files after two hours. Account reset first writes a protected, backup-excluded purge-pending marker; any deletion failure leaves the marker in place so the next cleanup removes every residual artifact regardless of age before clearing it. Recorder creation, export publication, and media access remain fenced while purge is pending.
- Voice session creation captures the current account generation and device identity. Token bootstrap/recovery cannot overwrite a newer account after an await, and any post-create identity or URL validation failure ends the created session with the exact registration that created it.
- Within the same account, every retry also has a distinct connection-attempt identity. Receive loops, delayed detail loads, media callbacks, and playout acknowledgements check that identity across asynchronous boundaries. Finalization does not cancel its own task; a local transport failure or server failure reason survives REST cleanup even when the summary/detail request fails. The terminal UI removes its countdown and replaces the reservation snapshot with settled server quota instead of reporting a completed lesson or treating the reserved allowance as consumed.

- Call and transcript remain mounted as one surface organized around one central orb. Gesture routing keeps vertical disclosure continuous between stable anchors: an upward orb drag reveals the full-screen transcript and a downward drag returns to the call. Transcript history owns its own scrolling and never changes disclosure. A short tap uses the acknowledged pause/resume operation. A stationary hold enters preparation at 0.35 seconds, displays the localized keep-holding-to-end instruction at 0.9 seconds, and invokes the existing explicit end callback once at 2.2 seconds; release, movement, cancellation, screen exit, or attempt change invalidates the hold before it can end another call. Tap, swipe, and hold ownership are mutually exclusive: a stationary release before hold preparation may tap, but release after preparation cancels the hold without toggling pause. Expected greeting, pending learner turn, and first audible output remain independent state, so overlapping input and exact silent-settlement receipts preserve truthful waiting without requiring a persistent status/control strip. Motion remains restrained and respects Reduce Motion. Each attempt starts collapsed; the lazy transcript retains its scroll position across reveal/hide and follows captions only at the latest edge. Terminal explanation and retry stay with the preserved conversation, including failures before the first tutor response. Light/dark colors, larger text, and equivalent VoiceOver actions preserve the same behavior.
- Saved-topic selection retains server authority without forcing magic speech. One exact server-read candidate can be reissued once after a persisted semantic `NONE`: only its immutable ID/path/revision enter a no-tool response, actual completed tutor audio must repeat the exact name, and that new playout boundary mints a fresh offer ID. A named, referential or contextual start on that offer is final and a successful ordinary selection immediately authorizes the first substantive question; it never asks for readiness again. An independently assessed `DECLINE_SAVED_TOPIC_OFFER` requires a meaningful final item with an actual spoken offer, carries no target/spoken IDs or authority, and skips the re-offer entirely. The reissued offer ID is also fenced from recursively scheduling itself, so rejection returns to normal discovery. Separately, a `list_studies` voice query that returns a verified empty page may retry once with a non-regex, trailing sentence-punctuation-only search key. The raw query runs first to preserve genuine punctuation-bearing titles, the exact effective query remains attached to candidate scope, and this read fallback grants no selection or mutation authority.

## Sync Model

- Backend sync stores settings, canonical QUESTION/VOICE_TUTOR records, private source Voice Tutor sessions/results, answer drafts, grading results, and topic statistics. A public voice payload contains only the selected exchange and learning feedback, never private session/turn/tree IDs or audio metadata.
- Backend record rows are retained without a per-user maximum. `GET /api/v1/records` and its search variant are bounded offset pages; iOS requests the next 30 rows only when the last loaded row approaches the viewport.
- Full Voice Tutor session history retains bounded `/api/v1/voice-tutor/sessions` pages. Canonical exchange records populate the existing Records cache; their original question/answer text is read-only and never becomes an answer draft. API/client body logging is suppressed for common records and public record/comment/feed routes as well as private sessions. Voice translations retain their existing source-hash/token stream contract and check the common record tombstone before requests, reads and completion.
- `GET /api/v1/records?studyId={nodeId}` applies the node filter in MySQL before counting and paging. Statistics node detail uses this additive filter so deep study trees never require downloading or client-filtering the user's full record history.
- API key backend sync is supported for the regular OpenAI key; admin keys are not supported.
- Backend settings sync uploads the regular OpenAI API key only when it changes or when backend settings need to be initialized.
- A backend device registration can be created without an APNs token so manual question generation, grading, settings, records, and stats can work before notification permission/token delivery.
- When APNs token registration later succeeds, the existing backend device is updated instead of creating a separate backend identity.
- If a local ungraded current question has an answer draft, remote current questions, Voice Tutor lifecycle updates, and Tutor Learning Result synchronization do not replace the active answer page.

## Push Model

- iPhone registers for remote notifications through `UIApplication`.
- iPhone app timers only run while the app process is active. For locked/background delivery, the app opportunistically pre-generates at most one pending question notification when entering background and schedules it for the configured interval. If a question notification is already pending, it does not create another. `BGAppRefresh` is also requested at the next due date, but iOS does not guarantee exact wake-up timing.
- The Spring Boot Kotlin backend is the production path for server-scheduled APNs delivery. It stores APNs tokens and schedules in MySQL, keeps user OpenAI keys encrypted at rest when provided, and stores a generated question with one `NOTIFICATION_REQUESTED` outbox event whose payload includes `shouldPush`. The notification listener persists the inbox row first, then publishes its ID through `notification.question-push.requested.v1` when push is enabled. An `@StreamListener` sends APNs alerts, while `app_notifications` stores claim, sent, and error state. Successful push messages use `StreamOptions.ACK` so bounded stream history remains available for operations; failures remain pending for `@StreamScheduler` auto-claim recovery.
- Scheduled delivery requires an APNs token. If a backend device exists without a token, the scheduler defers the due item instead of generating an undeliverable push.
- Push eligibility is checked twice: before publishing the push Stream event and again immediately before the APNs call. The final check reloads the current device, requires the queued user to remain attached with an active session, and uses the current database APNs token instead of the token captured in the Stream fields. A logout therefore invalidates already-queued delivery as well as later notification targeting.
- `devices.user_id` is the single current account owner for an iOS installation. Every session activation revokes active `user_devices` rows for other users on the same device, including anonymous-to-member and member-to-member switches. Notification target resolution also filters by the current device owner so historical inconsistent rows cannot route another account's push. Ownership or session rejection is acknowledged as a controlled non-delivery; only an actual Redis publication failure escapes the listener for Stream retry.
- iOS also calls `unregisterForRemoteNotifications()` on logout, removes pending and delivered notification-center entries, clears queued notification responses and device-token callbacks, and ignores any remote delivery that races with logout. A successful login calls `registerForRemoteNotifications()` again through the existing authorization flow; it does not require the user to grant permission a second time when authorization is still active.

## Community Identity

- Device credentials are used to register a backend device and bootstrap or refresh an access token.
- Access tokens carry both `user_id` and `device_id`; protected API calls resolve the current principal from those claims and the stored user-device mapping.
- Google Login links a verified Google subject to the registered device through `users` and `devices.user_id`.
- New Google and email users receive a cryptographically randomized `Adjective-Noun-####` display name. MySQL stores a generated, normalized key for non-anonymous users and enforces it with `uq_users_display_name_key`; creation retries a new candidate when a concurrent collision is detected.
- Anonymous installation rows are excluded from that display-name key so their internal `Buddy` placeholder can repeat. Profile edits use the same database constraint and return `DISPLAY_NAME_TAKEN` on conflict.
- User status is one of `ANONYMOUS`, `PENDING_TERMS`, `ACTIVE`, or `WITHDRAWN`. A newly authenticated account remains `PENDING_TERMS` until all required agreements are accepted; that transition to `ACTIVE` is also the referral reward boundary. Account deletion transactionally changes the member to `WITHDRAWN`, scrubs login/secret/profile fields, revokes active sessions, detaches its devices, reconnects the current device to an anonymous user, and appends an `ACCOUNT_WITHDRAWN` row to `redis_event_outbox`.
- The outbox recovery job publishes `ACCOUNT_WITHDRAWN` to the domain Redis Stream. `AccountWithdrawalStreamListener` processes it at least once and acknowledges only after idempotent SQL cleanup and profile-photo deletion complete. Failures remain pending and are reclaimed after the idle lease; replays are safe because deletes are idempotent and device-scoped rows are bounded by the event's `withdrawnAt` cutoff.
- Public question rows expose only the author's public profile fields: display name, bio, pixel-avatar symbol, and color seed. The iOS app renders the avatar locally and does not upload or display user photos.
- Public visibility requires both record-level `is_public` and account-level `allow_public_questions`, a completed answered exchange, a live author and the existing block rules. Voice records obey the same actions/ID. V105 explicitly keeps all historical voice records and pre-migration calls private; enabling an account's sharing preference cannot silently publish them.
- Reports are stored in MySQL and can optionally be emailed to the operator Gmail through SMTP settings.

## Topic Statistics

- Topic grouping uses `TopicGrouping.normalizedKey`.
- The key removes case, spacing, hyphen, underscore, punctuation, width, and diacritic differences.
- Simple camelCase boundaries are separated before normalization.
- The displayed topic is the most frequent/recent label in the merged group.
- Topic range is estimated from difficulty level and score, then widened by small sample size and conflicting evidence.
- Backend topic statistics are a materialized read model in `user_stats`.
- `questions` remains the source of truth. Answer grading and record deletion mark only the affected `(user_id, stat_date, topic_key, difficulty_level)` bucket in `user_stats_dirty_keys`.
- The refresh job processes dirty keys in bounded batches and recomputes each bucket with MySQL aggregation instead of loading all questions into application memory.
- Dirty key processing is at-least-once and idempotent: recalculating the same bucket deletes and reinserts only that bucket's `user_stats` rows.
- Refresh claims dirty rows with `FOR UPDATE SKIP LOCKED`; if the process crashes before commit, the transaction rolls back and the dirty rows remain for the next run.
- Refresh deletes a dirty row only when its `updated_at` still matches the claimed value. If a new answer/delete updates the same bucket during refresh, the dirty row is kept and retried in a later batch.
- H2/test environments fall back to a full rebuild path; production MySQL uses incremental dirty-key refresh.
- Tree growth does not rewrite `user_stats`. `StudyGrowthStatsPort` reads questions created or answered in the requested period by stable `study_id`, and `StudyGrowthService` joins them to the current `StudyPort` tree. Only graded answers feed ability and trend calculations; ungraded questions remain available for the completion denominator.
- A direct node needs six graded answers for growth. The previous and recent windows never overlap and contain three to five samples each.
- Parent and root estimates include descendant nodes, cap each node's weight at five answers, and report measured-node coverage separately from total subtree size.
- `GET /api/v1/stats/studies` returns root summaries plus a flat node list containing `studyId`, `parentStudyId`, and `rootStudyId`. Each root also carries a normalized five-axis profile: mean score, mean answered difficulty, completed/generated ratio, answered-topic coverage, and deepest answered tree coverage.
- Statistics tab re-selection is freshness-gated for one minute while pull-to-refresh bypasses that gate. Rolling growth periods are expressed as exclusive UTC day bounds, and backend plus iOS topic selection use explicit deterministic tie-breaks so identical read-model data produces identical cards.
- iOS reconstructs a stable pre-order list for the selected root. It lazily renders that full depth-indented list below the radar instead of requiring repeated branch navigation.
- The iOS root overview maps every study's previous and current estimates onto the same 1–10 axis. The help sheet documents the exact window and weighting rules instead of asking users to infer them from the chart.
- Active iOS statistics graphs share one pixel-game presentation policy. Root rows use a ten-tile 1–10 meter plus a compact stepped trend, detail trends use a Canvas-rendered stepped line with square markers, and activity uses localized square day cells. The circular study-tree geometry and all score/growth semantics remain unchanged; visual cells are grouped into concise accessibility summaries instead of becoming hundreds of VoiceOver stops.

## Subscription, Entitlement, And Quota

- `invoices`/`invoice_events` and `payments`/`payments_history` are the financial ledger. A verified charge completes its normal invoice; later entitlement projection failure does not rewrite financial truth and never automatically requests a refund.
- `billing_accounts` retains one `appAccountToken` owner. Account deletion anonymizes the record and prevents that purchase chain being restored to another BuddyStudy account.
- `subscription_events` is append-only and idempotent. `subscriptions` projects access and renewal independently per original transaction, and `user_entitlement_projection` chooses the highest valid tier without adding duplicate subscription limits.
- `user_memberships` carries non-StoreKit grants such as the one-month referral reward. Effective access resolves the highest active tier across the paid entitlement projection and these grants; a referral never creates an invoice, payment, or auto-renewing subscription.
- `user_membership_tiers` is the plan catalog and owns separate base monthly question and Voice Tutor limits.
- The default question allowance is 30 for TIER1, 300 for TIER2, and 1,000 for TIER3. The default Voice Tutor allowance is zero seconds for TIER1 and 3,600 seconds (60 minutes) for both TIER2 and TIER3. TIER2 and TIER3 each expose one monthly Apple auto-renewable subscription. Disabled legacy product mappings remain readable only for billing reconciliation.
- The iOS membership screen renders the StoreKit-localized title/price context together with the server-owned monthly question allowance, Voice Tutor allowance, monthly duration, automatic-renewal disclosure, and localized Terms of Use and Privacy Policy links before the purchase action.
- App Store Connect subscription metadata is tracked in `app-store/billing/subscriptions.json`. Korean prices are the base prices and Apple equalized prices are configured for every available storefront.
- A user-initiated purchase creates an `INVOICE_CREATED` event and `NORMAL/WAITING` invoice (`phase=PREPARED`) before the StoreKit sheet opens. RevenueCat returns the completed StoreKit transaction identifier; iOS confirms it against that exact invoice through `POST /api/v1/billing/invoices/{invoiceNumber}/confirm`.
- The backend verifies the transaction through RevenueCat API v2 and advances the derived phase `PREPARED -> VERIFIED -> FULFILLED`. The confirmation request returns 2xx only after payment settlement, `fulfilled_at`, entitlement, and quota are readable. A failure preserves verified financial evidence for idempotent recovery.
- The iOS Debug launch action attaches `StudyMateDev.storekit`, so local Xcode purchases exercise the same pending-invoice, transaction-sync, fulfillment, and event-ledger path as App Store purchases. The dev backend accepts `XCODE` and `SANDBOX`; production accepts Apple-signed environments and explicitly rejects Xcode-local transactions.
- RevenueCat owns StoreKit transaction completion with the App Store `appl_` public SDK key in every iOS build. Apple determines Sandbox versus Production from the transaction environment. The stable BuddyStudy `appAccountToken` is its App User ID. Direct transaction confirmation and authenticated RevenueCat webhooks share one verified-payment use case and deduplicate by provider transaction ID, making arrival order and retries harmless. Direct Apple JWS remains a backward-compatible recovery input.
- Invoice projection status is only `WAITING`, `COMPLETED`, or `FAILED`; granular verification, cancellation, and refund steps are event/payment/action states. `COMPLETED` means authenticated payment evidence committed.
- Refunds create linked `REFUND` invoices instead of mutating the completed `NORMAL` invoice. Apple confirmation completes the refund invoice; decline or reversal fails it.
- `user_quota` is the single current-state quota projection per user. It owns the anchor, current period boundaries, effective tier and base limit, current-period bonus, committed and reserved counters, derived remaining capacity, policy version, and optimistic version.
- `user_quota_history` is append-only. The current-row update and its history entry commit in one transaction for period rollover, reserve, commit, release, bonus, migration, and effective plan changes. A unique event ID and resulting-version chain make replays harmless and provide an auditable counter and plan-change trail; `user_quota` remains the authoritative full current snapshot.
- `quota_reservations` remains the exactly-once identity for question-generation Sagas. It snapshots the period in which the reservation was accepted, so a commit or release delivered after a natural rollover completes the old-period history without corrupting the new current row.
- `user_voice_quota` is the independent current-state seconds projection for Voice Tutor. `voice_tutor_sessions` supplies its reservation/settlement identity, so concurrent starts, duplicate end requests, disconnect recovery, and an old-period terminal settlement cannot charge or release the current period twice.
- Month-end anchors use the target month's last valid day without drift. For example, a January 31 anchor resets on February 28 (or 29) and then March 31. Question and Voice Tutor quotas apply this calendar rule independently.
- The first verified paid purchase may replace the question quota's account-created anchor with the immutable earliest `purchasedAt`, carrying its existing counters into the recalculated window. The Voice Tutor anchor remains the account creation time. Later upgrades, downgrades, renewals, cancellations, expirations, refunds, and resubscriptions move neither established anchor.
- Question remaining capacity is `max(0, base + current-period bonus - committed - reserved)`; Voice Tutor remaining capacity is `max(0, base - used - reserved)`. Billing fulfillment applies the question limit and its invoice-scoped history event transactionally. Voice Tutor instead reconciles the effective tier and plan-catalog base on the next voice access while preserving its used and reserved seconds. A requested downgrade keeps the higher entitlement until its renewal boundary, and either domain clamps remaining capacity to zero when preserved usage meets the later lower base.
- An active referral grant makes Pro (`TIER2`) effective unless a higher valid tier already wins. Question quota applies that effective tier through its existing fulfillment boundary; Voice Tutor observes it through the same lazy tier/base reconciliation. Both preserve current-period usage and active reservations.
- The managed `user-quota-rollover` job remains question-only and advances due `user_quota` rows every minute with bounded locking. Voice Tutor has no separate rollover batch in this release: status, history/detail, and session-start paths reconcile expired sessions and lazily advance `user_voice_quota` to the account-anchored window containing the current UTC instant. Both domains retain request-time correctness during scheduler delay or process downtime. Legacy membership/usage and policy-v4 question quota tables are migration inputs only and are not a read authority.
- The monitoring Users & Quotas page proxies admin APIs through the authenticated monitoring origin. It does not persist backend admin tokens outside the browser session.
- User search is bounded to 100 rows per API call and the UI uses 20-row pages.

## Managed Batch Operations

- Business schedulers implement `ManagedJob` and execute through `ManagedJobExecutionUseCase`. The job contract owns an operator-facing name and description in addition to the stable machine name.
- `scheduled_jobs` is the enablement, schedule-display, timeout, and lock-policy registry. Spring scheduling annotations remain the execution trigger, so migrations must keep the displayed schedule value aligned with the configured trigger.
- Every managed execution writes `RUNNING` and then `SUCCESS`, `FAILED`, or `SKIPPED` to `scheduled_job_runs`, including trigger type, start/finish time, duration, bounded result summary or error, retry source, and initiator.
- A MySQL advisory lock prevents concurrent execution of the same managed job across overlapping backend instances. Job work remains idempotent because a lock does not by itself guarantee exactly-once side effects.
- `user-quota-rollover` runs once per minute to materialize natural monthly resets even when a user is idle. Its deterministic per-user period event and row lock make overlapping workers harmless; request-time fallback remains mandatory rather than relying on scheduler timeliness for correctness.
- The administrator status API pages the `scheduled_jobs` registry before loading latest-run and last-success snapshots for only the requested job names. Configured monitored jobs missing from the registry remain synthetic trailing rows in the same stable paginated result. Readiness freshness applies only to the configured frequent critical subset, so daily correction jobs remain visible without being incorrectly marked stale by the global 15-minute readiness threshold.
- Monitoring `Manage > Batch Jobs` uses the authenticated admin APIs for independently paginated job status and run history, run details, and explicit retry. The standalone Admin `Batch Jobs` navigation links to this detailed workspace. Redis Stream consumer polling and `@StreamScheduler` pending-entry recovery remain pipeline operations and are inspected under `Manage > Redis Streams`, not recorded as high-frequency batch runs.

## Production Incident Auto-Fix

- Grafana remains the only producer of backend ERROR notifications. Its provisioned contact point fans out to Slack and an HMAC-signed, private Monitoring webhook; the backend application has no Slack or GitHub credential.
- `buddystudy-incident-receiver` has no host port. It deduplicates each `(Grafana fingerprint, startsAt)` on persistent disk, reads a bounded redacted ERROR context from Loki, joins the latest successful backend deployment SHA, and sends `codex-incident-autofix` to the source repository.
- The GitHub workflow separates trust boundaries: the Codex job has source read access plus its dedicated OpenAI key, the verification job applies only the serialized patch and runs the full backend test suite, and only the final job receives branch/PR write permission.
- Generated changes are restricted to backend code and directly relevant documentation. Successful verification creates a labeled Draft PR; automatic merge, release, tagging, deployment, and production access are prohibited. The full contract and recovery behavior are documented in `docs/observability/codex-incident-autofix.md`.

## Build And Verification

Recommended local checks:

```sh
xcodebuild -project StudyMate.xcodeproj -scheme StudyMateiOS -configuration Debug -destination 'generic/platform=iOS' -derivedDataPath build/iOSDeviceDerivedData CODE_SIGNING_ALLOWED=NO build
```

Use real-device builds when changing push, entitlements, or background refresh behavior.
