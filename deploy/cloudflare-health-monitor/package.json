{
  "name": "buddystudy-health-monitor",
  "private": true,
  "type": "module",
  "scripts": {
    "configure:kv": "node scripts/set-kv-namespace.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node scripts/check-config.js && node scripts/check-workflow.js && wrangler deploy --dry-run",
    "manual:check": "node scripts/manual-check.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
