apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy
---
apiVersion: v1
data:
  ADMIN_ANALYTICS_DATABASE_URL: jdbc:postgresql://buddystuddy-postgres:5432/buddystuddy_aggregation
  ADMIN_ANALYTICS_DATABASE_USERNAME: buddystuddy
  APP_PORT: "8080"
  BUDDYSTUDDY_TRANSLATION_BASE_URL: http://buddystuddy-libretranslate:5000
  DATABASE_URL: jdbc:postgresql://buddystuddy-postgres:5432/buddystuddy
  DATABASE_USERNAME: buddystuddy
  ENABLE_OPENAPI_DOCS: "false"
  FLYWAY_LOCATIONS: filesystem:/app/db/migration
  GOOGLE_IOS_CLIENT_ID: 328281669925-evuebplih3vir99daj4lapj1javqsmsr.apps.googleusercontent.com
  MAX_PENDING_PER_STUDY: "1"
  PUSH_STREAM_PREFIX: push-v1
  REACTION_STREAM_COORDINATOR_BASE_URL: https://coordinator.ghkdqhrbals.org
  REACTION_STREAM_COORDINATOR_USERNAME: admin
  REACTION_STREAM_ENABLED: "true"
  REDIS_HOST: buddystuddy-redis
  REDIS_PORT: "6379"
  REDIS_SSL: "false"
  SCHEDULER_BATCH_SIZE: "50"
  SCHEDULER_ENABLED: "true"
  SPRING_CLOUD_AWS_SECRETSMANAGER_ENABLED: "false"
  SPRING_DATA_REDIS_CLUSTER_NODES: buddystuddy-redis-0.buddystuddy-redis-headless:6379,buddystuddy-redis-1.buddystuddy-redis-headless:6379,buddystuddy-redis-2.buddystuddy-redis-headless:6379
  SPRING_PROFILES_ACTIVE: prod
  VIEW_STREAM_PREFIX: view-v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-backend-config
  namespace: buddystuddy
---
apiVersion: v1
data:
  01-create-aggregation-db.sql: |
    SELECT 'CREATE DATABASE buddystuddy_aggregation'
    WHERE NOT EXISTS (
      SELECT FROM pg_database WHERE datname = 'buddystuddy_aggregation'
    )\gexec
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres-init
  namespace: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-admin-frontend
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-admin-frontend
  namespace: buddystuddy
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app.kubernetes.io/name: buddystuddy-admin-frontend
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-backend
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-backend
  namespace: buddystuddy
spec:
  ports:
  - name: http
    port: 8080
    targetPort: http
  selector:
    app.kubernetes.io/name: buddystuddy-backend
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-libretranslate
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-libretranslate
  namespace: buddystuddy
spec:
  ports:
  - name: http
    port: 5000
    targetPort: http
  selector:
    app.kubernetes.io/name: buddystuddy-libretranslate
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-postgres
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres
  namespace: buddystuddy
spec:
  ports:
  - name: postgres
    port: 5432
    targetPort: postgres
  selector:
    app.kubernetes.io/name: buddystuddy-postgres
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis
  namespace: buddystuddy
spec:
  ports:
  - name: redis
    port: 6379
    targetPort: redis
  selector:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis-headless
  namespace: buddystuddy
spec:
  clusterIP: None
  ports:
  - name: redis
    port: 6379
    targetPort: redis
  - name: cluster-bus
    port: 16379
    targetPort: cluster-bus
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
---
apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-postgres
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres-data
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 20Gi
  hostPath:
    path: /Users/gyuminhwangbo/data/buddystudy/db/postgres
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/ordinal: "0"
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis-data-0
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /Users/gyuminhwangbo/data/buddystudy/redis/redis-0
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/ordinal: "1"
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis-data-1
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /Users/gyuminhwangbo/data/buddystudy/redis/redis-1
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/ordinal: "2"
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis-data-2
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /Users/gyuminhwangbo/data/buddystudy/redis/redis-2
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-libretranslate-data
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres-backups
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres-data
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-postgres
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: data-buddystuddy-redis-0
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-redis
      app.kubernetes.io/ordinal: "0"
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: data-buddystuddy-redis-1
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-redis
      app.kubernetes.io/ordinal: "1"
  storageClassName: buddystuddy-local
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/part-of: buddystuddy
  name: data-buddystuddy-redis-2
  namespace: buddystuddy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-redis
      app.kubernetes.io/ordinal: "2"
  storageClassName: buddystuddy-local
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-admin-frontend
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-admin-frontend
  namespace: buddystuddy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-admin-frontend
      app.kubernetes.io/part-of: buddystuddy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-admin-frontend
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - env:
        - name: BACKEND_UPSTREAM
          value: http://buddystuddy-backend:8080
        - name: BACKEND_PROXY_HOST
          value: buddystuddy-backend
        image: ghcr.io/ghkdqhrbals/buddy-studdy-admin-frontend:latest
        imagePullPolicy: Always
        name: admin-frontend
        ports:
        - containerPort: 80
          name: http
        readinessProbe:
          httpGet:
            path: /admin/
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            memory: 128Mi
          requests:
            cpu: 25m
            memory: 64Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-backend
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-backend
  namespace: buddystuddy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-backend
      app.kubernetes.io/part-of: buddystuddy
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-backend
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: buddystuddy-backend-config
        - secretRef:
            name: buddystuddy-backend-secret
        image: ghcr.io/ghkdqhrbals/buddy-studdy-backend:latest
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 5
        name: backend
        ports:
        - containerPort: 8080
          name: http
        readinessProbe:
          failureThreshold: 12
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 3
        resources:
          limits:
            memory: 768Mi
          requests:
            cpu: 100m
            memory: 256Mi
      initContainers:
      - command:
        - sh
        - -c
        - |
          until redis-cli -a "$REDIS_PASSWORD" -h buddystuddy-redis-0.buddystuddy-redis-headless -p 6379 cluster info | grep -q "cluster_state:ok"; do
            sleep 2
          done
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: REDIS_PASSWORD
              name: buddystuddy-backend-secret
        image: redis:7-alpine
        imagePullPolicy: IfNotPresent
        name: wait-for-redis-cluster
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-libretranslate
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-libretranslate
  namespace: buddystuddy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-libretranslate
      app.kubernetes.io/part-of: buddystuddy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-libretranslate
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - env:
        - name: LT_LOAD_ONLY
          value: en,ko
        - name: LT_DISABLE_WEB_UI
          value: "true"
        - name: LT_WORKERS
          value: "1"
        image: libretranslate/libretranslate:latest
        imagePullPolicy: IfNotPresent
        name: libretranslate
        ports:
        - containerPort: 5000
          name: http
        readinessProbe:
          httpGet:
            path: /languages
            port: http
          initialDelaySeconds: 20
          periodSeconds: 20
        volumeMounts:
        - mountPath: /home/libretranslate/.local/share
          name: data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: buddystuddy-libretranslate-data
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-postgres
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres
  namespace: buddystuddy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-postgres
      app.kubernetes.io/part-of: buddystuddy
  serviceName: buddystuddy-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-postgres
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - env:
        - name: POSTGRES_DB
          value: buddystuddy
        - name: POSTGRES_USER
          value: buddystuddy
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              key: DATABASE_PASSWORD
              name: buddystuddy-backend-secret
        image: postgres:16-alpine
        imagePullPolicy: IfNotPresent
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - buddystuddy
            - -d
            - buddystuddy
          initialDelaySeconds: 30
          periodSeconds: 20
        name: postgres
        ports:
        - containerPort: 5432
          name: postgres
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - buddystuddy
            - -d
            - buddystuddy
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: data
        - mountPath: /docker-entrypoint-initdb.d
          name: init-scripts
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: buddystuddy-postgres-data
      - configMap:
          name: buddystuddy-postgres-init
        name: init-scripts
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis
  namespace: buddystuddy
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystuddy-redis
      app.kubernetes.io/part-of: buddystuddy
  serviceName: buddystuddy-redis-headless
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-redis
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - command:
        - sh
        - -c
        - |
          exec redis-server \
            --port 6379 \
            --bind 0.0.0.0 \
            --protected-mode no \
            --appendonly yes \
            --dir /data \
            --requirepass "$REDIS_PASSWORD" \
            --masterauth "$REDIS_PASSWORD" \
            --cluster-enabled yes \
            --cluster-config-file nodes.conf \
            --cluster-node-timeout 5000 \
            --cluster-announce-hostname "$(hostname).buddystuddy-redis-headless.buddystuddy.svc.cluster.local" \
            --cluster-announce-port 6379 \
            --cluster-announce-bus-port 16379
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: REDIS_PASSWORD
              name: buddystuddy-backend-secret
        image: redis:7-alpine
        imagePullPolicy: IfNotPresent
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - redis-cli -a "$REDIS_PASSWORD" ping | grep PONG
          initialDelaySeconds: 30
          periodSeconds: 20
        name: redis
        ports:
        - containerPort: 6379
          name: redis
        - containerPort: 16379
          name: cluster-bus
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - redis-cli -a "$REDIS_PASSWORD" ping | grep PONG
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - mountPath: /data
          name: data
  volumeClaimTemplates:
  - metadata:
      labels:
        app.kubernetes.io/part-of: buddystuddy
      name: data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 5Gi
      storageClassName: buddystuddy-local
---
apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-postgres-backup
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-postgres-backup
  namespace: buddystuddy
spec:
  failedJobsHistoryLimit: 3
  jobTemplate:
    metadata:
      labels:
        app.kubernetes.io/part-of: buddystuddy
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: buddystuddy-postgres-backup
            app.kubernetes.io/part-of: buddystuddy
        spec:
          containers:
          - command:
            - sh
            - -c
            - |
              set -euo pipefail
              ts="$(date -u +%Y%m%dT%H%M%SZ)"
              mkdir -p /backups
              pg_dump -Fc -h "${PGHOST}" -U "${PGUSER}" -d buddystuddy > "/backups/buddystuddy-${ts}.dump"
              pg_dump -Fc -h "${PGHOST}" -U "${PGUSER}" -d buddystuddy_aggregation > "/backups/buddystuddy-aggregation-${ts}.dump"
              find /backups -type f -name "buddystuddy*.dump" -mtime +"${BACKUP_RETENTION_DAYS}" -delete
            env:
            - name: PGHOST
              value: buddystuddy-postgres
            - name: PGUSER
              value: buddystuddy
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  key: DATABASE_PASSWORD
                  name: buddystuddy-backend-secret
            - name: BACKUP_RETENTION_DAYS
              value: "14"
            image: postgres:16-alpine
            imagePullPolicy: IfNotPresent
            name: backup
            volumeMounts:
            - mountPath: /backups
              name: backups
          restartPolicy: OnFailure
          volumes:
          - name: backups
            persistentVolumeClaim:
              claimName: buddystuddy-postgres-backups
  schedule: 0 18 * * *
  successfulJobsHistoryLimit: 3
---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app.kubernetes.io/name: buddystuddy-redis
    app.kubernetes.io/part-of: buddystuddy
  name: buddystuddy-redis-cluster-init
  namespace: buddystuddy
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystuddy-redis-cluster-init
        app.kubernetes.io/part-of: buddystuddy
    spec:
      containers:
      - command:
        - sh
        - -c
        - |
          set -eu
          nodes="
          buddystuddy-redis-0.buddystuddy-redis-headless.buddystuddy.svc.cluster.local:6379
          buddystuddy-redis-1.buddystuddy-redis-headless.buddystuddy.svc.cluster.local:6379
          buddystuddy-redis-2.buddystuddy-redis-headless.buddystuddy.svc.cluster.local:6379
          "
          for node in $nodes; do
            until redis-cli -a "$REDIS_PASSWORD" -h "${node%:*}" -p "${node#*:}" ping | grep -q PONG; do
              sleep 2
            done
          done
          if redis-cli -a "$REDIS_PASSWORD" -h buddystuddy-redis-0.buddystuddy-redis-headless.buddystuddy.svc.cluster.local cluster info | grep -q "cluster_state:ok"; then
            echo "Redis cluster already initialized"
            exit 0
          fi
          yes yes | redis-cli -a "$REDIS_PASSWORD" --cluster create $nodes --cluster-replicas 0
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: REDIS_PASSWORD
              name: buddystuddy-backend-secret
        image: redis:7-alpine
        imagePullPolicy: IfNotPresent
        name: redis-cluster-init
      restartPolicy: OnFailure
