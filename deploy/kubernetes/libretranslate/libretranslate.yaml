apiVersion: v1
kind: Service
metadata:
  name: buddystudy-libretranslate
  labels:
    app.kubernetes.io/name: buddystudy-libretranslate
spec:
  selector:
    app.kubernetes.io/name: buddystudy-libretranslate
  ports:
    - name: http
      port: 5000
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buddystudy-libretranslate
  labels:
    app.kubernetes.io/name: buddystudy-libretranslate
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystudy-libretranslate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystudy-libretranslate
    spec:
      containers:
        - name: libretranslate
          image: libretranslate/libretranslate:v1.9.6@sha256:1de2d7056bb8ad607a412f4563d9abe324ff632b43b5be9428bcc8e213aebb32
          imagePullPolicy: IfNotPresent
          env:
            - name: LT_LOAD_ONLY
              value: en,ko,ja
            - name: LT_DISABLE_WEB_UI
              value: "true"
            - name: LT_THREADS
              value: "1"
            - name: LT_UPDATE_MODELS
              value: "true"
          ports:
            - name: http
              containerPort: 5000
          readinessProbe:
            httpGet:
              path: /languages
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /home/libretranslate/.local/share
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: buddystudy-libretranslate-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buddystudy-libretranslate-data
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
