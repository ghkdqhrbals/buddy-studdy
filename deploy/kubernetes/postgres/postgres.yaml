apiVersion: v1
kind: ConfigMap
metadata:
  name: buddystudy-postgres-init
data:
  01-create-aggregation-db.sql: |
    SELECT 'CREATE DATABASE buddystudy_aggregation'
    WHERE NOT EXISTS (
      SELECT FROM pg_database WHERE datname = 'buddystudy_aggregation'
    )\gexec
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: buddystudy-postgres-data
  labels:
    app.kubernetes.io/name: buddystudy-postgres
spec:
  capacity:
    storage: 20Gi
  accessModes: ["ReadWriteOnce"]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: buddystudy-local
  hostPath:
    path: /Users/gyuminhwangbo/data/buddystudy/db/postgres
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buddystudy-postgres-data
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: buddystudy-local
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystudy-postgres
---
apiVersion: v1
kind: Service
metadata:
  name: buddystudy-postgres
  labels:
    app.kubernetes.io/name: buddystudy-postgres
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: buddystudy-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      nodePort: 30432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: buddystudy-postgres
  labels:
    app.kubernetes.io/name: buddystudy-postgres
spec:
  serviceName: buddystudy-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystudy-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystudy-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
              hostPort: 5432
          env:
            - name: POSTGRES_DB
              value: buddystudy
            - name: POSTGRES_USER
              value: buddystudy
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: buddystudy-backend-secret
                  key: DATABASE_PASSWORD
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "buddystudy", "-d", "buddystudy"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "buddystudy", "-d", "buddystudy"]
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init-scripts
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: buddystudy-postgres-data
        - name: init-scripts
          configMap:
            name: buddystudy-postgres-init
