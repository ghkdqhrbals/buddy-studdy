apiVersion: v1
kind: Service
metadata:
  name: buddystudy-admin-frontend
  labels:
    app.kubernetes.io/name: buddystudy-admin-frontend
spec:
  selector:
    app.kubernetes.io/name: buddystudy-admin-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buddystudy-admin-frontend
  labels:
    app.kubernetes.io/name: buddystudy-admin-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: buddystudy-admin-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buddystudy-admin-frontend
    spec:
      containers:
        - name: admin-frontend
          image: ghcr.io/ghkdqhrbals/buddy-study-admin-frontend:latest
          imagePullPolicy: Always
          env:
            - name: BACKEND_UPSTREAM
              value: http://buddystudy-backend:8080
            - name: BACKEND_PROXY_HOST
              value: buddystudy-backend
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /admin/
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 128Mi

